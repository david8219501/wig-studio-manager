# סיכום: תיקון bidi בהצגת אימייל/טלפון - dir="ltr" חסר

## הבאג שדווח

בכותרת `ClientDrawer.tsx` (הפאנל הנשלף של כרטיס לקוחה) - טלפון+אימייל
הוצגו באותו `<p className="mono">` בלי `dir="ltr"` מפורש. בהקשר RTL
(עברי), טקסט לועזי מעורב (במיוחד אימייל עם `@` ואותיות) עלול להיהפך
ויזואלית ע"י אלגוריתם ה-bidi - בדיוק כפי שדווח ("gmail.com@84873553"
במקום "84873553@gmail.com"). אותה בעיה בדיוק שכבר תוקנה בעבר בטבלת
ההוצאות (`Expenses.tsx`).

**תוקן:** נוסף `dir="ltr"` ל-`<p>` כולו בכותרת (עוטף גם טלפון וגם
אימייל יחד - הם באותה שורה ומופרדים ב-"•").

## חיפוש גלובלי - עוד 5 מקומות נמצאו ותוקנו

בדקתי כל הצגת `.phone`/`.email`/`clientPhone` בכל קובצי ה-TSX
בפרויקט (לא רק כתיבה/סינון פנימיים - רק תצוגה בפועל למשתמשת):

- **`NewOrderWizard.tsx`** (2 מקומות) - שם לקוחה + טלפון בסוגריים
  בשלב "בחירת לקוחה" (גם ל-`preselectedClient` וגם ברשימת החיפוש) -
  נוסף `<span dir="ltr">` סביב הטלפון בלבד (לא סביב כל השורה, כדי
  לא להפוך את סדר השם העברי).
- **`SellShowroomStockModal.tsx`** - אותו תבנית בדיוק (שם + טלפון
  ל-`preselectedClient` בטופס מכירת פאת תצוגה).
- **`Calendar.tsx`** - שורה שלישית שהתגלתה (שתיים מתוך שלוש כבר היו
  מתוקנות מסבב קודם: `apt.phone`, `selectedClientForPanel.phone`) -
  רשימת החיפוש/בחירת לקוחה בטופס "קביעת פגישה חדשה" (`{c.name}
  ({c.phone})`) לא הייתה מתוקנת - נוסף `<span dir="ltr">` סביב הטלפון.
- **`OrderDetailsPanel.tsx`** - `order.clientPhone` בכותרת הפאנל,
  ב-`<p>` נפרד ומבודד - נוסף `dir="ltr"` ישירות על האלמנט.

## מה כבר היה תקין - לא נדרש שינוי

- **`Clients.tsx`** - טבלת הלקוחות הראשית: גם עמודת הטלפון וגם עמודת
  האימייל כבר עם `dir="ltr"` על ה-`<td>`.
- **שדות קלט טלפון/אימייל** (`AddClientModal.tsx`, `Calendar.tsx` -
  טופס "הוספת לקוחה חדשה") - כולם כבר עם `dir="ltr"` על ה-`<input>`.
- **תפריטי `<select><option>`** (`QuickRetailSaleModal.tsx`,
  `SellShowroomStockModal.tsx` - בחירת לקוחה מרשימה): מכילים `{c.name}
  ({c.phone})` בלי `dir="ltr"` - **נבדק במפורש ולא תוקן בכוונה**:
  `<option>` הוא טקסט בלבד (לא ניתן לעטוף חלק ממנו ב-`<span>`), וטלפון
  הוא רצף ספרות בלבד (בלי אותיות) - ספרות הן "European Number" באלגוריתם
  ה-bidi ותמיד שומרות על סדר פנימי נכון גם בתוך הקשר RTL, בשונה
  מאימייל (שמכיל אותיות - תווי כיוון חזק). הוספת `dir="ltr"` על כל
  ה-`<option>` הייתה הופכת גם את יישור השם העברי - סיכון להחמרה
  ויזואלית בלי תועלת אמיתית. לכן הושאר כפי שהוא.

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - אין שגיאות/אזהרות חדשות בכל 5 הקבצים שנערכו.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית שכתובת
  האימייל בכותרת `ClientDrawer.tsx` מוצגת כעת בכיוון הנכון.

## הערה על git status

`summary2.md`/`summary3.md`/`summary4.md` עדיין מופיעים כ-`deleted`
ממשימות קודמות - לא נגעתי בהם, נשארים מחוץ לקומיט הזה.
