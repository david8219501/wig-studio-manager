# סיכום: 2 תיקונים שנפתחו כ"לא בוצעו" ב-status-check.md

## 1. "הכנסות החודש" בדשבורד - רווח נטו במקום הכנסה גולמית

`Dashboard.tsx`: `thisMonthRevenue`/`lastMonthRevenue` השתמשו קודם ב-
`sum + (o.totalPrice || 0)` (הכנסה גולמית). הוחלף ל-`sum +
calculateOrderProfit(o)` - אותה פונקציה בדיוק שכבר משמשת את עמודת
"רווח" ב-`Sales.tsx` (`src/utils/orderProfit.ts`), כדי ששני המסכים
יציגו מספרים עקביים. `revenueTrendPct` (המגמה לעומת חודש שעבר) נותר
ללא שינוי לוגי - הוא כבר חישב יחס בין שני הערכים, שעכשיו שניהם רווח
נטו במקום הכנסה גולמית.

לא שונו: גרף ההכנסות ל-6 חודשים (`revenueChartData`) ופילוח לפי סוג
עבודה (`servicesBreakdown`) - לא התבקשו, ממשיכים להציג הכנסה גולמית
כפי שהיו.

**דעתי לגבי הכותרת "הכנסות החודש":** הייתי משנה אותה ל**"רווח החודש"**
- הכותרת הנוכחית מטעה עכשיו (המספר מתחתיה הוא רווח, לא הכנסה), ובכל
הקבצים האחרים באתר (`Sales.tsx`) יש כבר הפרדה ברורה בין "הכנסות"
(`totalRevenue`, גולמי) ל"רווח" (`totalProfit`, נטו) - כדאי לשמור על
אותה מוסכמת מינוח גם בדשבורד כדי לא לבלבל. **לא שיניתי את הכותרת בפועל**
- ממתין לאישור שלך לפני נגיעה בטקסט המוצג למשתמשת.

**קובץ:** `src/pages/Dashboard/Dashboard.tsx`

## 2. עריכת סטטוס ב-OrderDetailsPanel.tsx

הקבועים `OTHER_STATUS`/`KNOWN_STATUSES`/`STATUS_SELECT_OPTIONS` היו
מקומיים ל-`Sales.tsx` (לא מיוצאים) - הועברו למקום משותף חדש,
**`src/utils/orderStatus.ts`**, ושני הקבצים מייבאים משם עכשיו (לא
כפילות קוד). `Sales.tsx` לא שונה התנהגותית - רק שינוי מקור הייבוא.

ב-`OrderDetailsPanel.tsx`: ה-`<span>` הסטטי הוחלף ב-`CustomSelect`
ניתן לעריכה בכותרת הפאנל, **אותו דפוס מדויק** כמו ב-`Sales.tsx` (כולל
"אחר / הוסף חדש" עם שדה טקסט חופשי, ושמירת סטטוס טקסט-חופשי קיים
כפי-שהוא בפתיחה חוזרת - לא "אחר" ריק). שינוי נשמר מיד עם `updateDoc`.

**החרגה מכוונת:** כשההזמנה כבר מבוטלת (`status === "בוטלה"`, מהתיקון
הקודם) - חזרה ל-badge סטטי בלי אפשרות עריכה, כי אין הגיון לשנות סטטוס
של הזמנה שבוטלה דרך ה-select הרגיל (יש לזה כבר נתיב ייעודי משלה - כפתור
"ביטול הזמנה").

**קבצים:** `src/utils/orderStatus.ts` (חדש), `src/pages/Sales/Sales.tsx`,
`src/components/orders/OrderDetailsPanel.tsx`,
`src/components/orders/OrderDetailsPanel.css`

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - אין אזהרות/שגיאות חדשות בשום קובץ שנגעתי בו (רק
  אזהרות `react-hooks/set-state-in-effect` קיימות מראש על `useEffect`-
  ים שלא נגעתי בהם).
- **וידוא בפועל בקוד** (לא רק "אמור להיות מתוקן") - חזרתי על אותה בדיקת
  `status-check.md` המקורית, ראה שם: שני הפריטים עברו ל-✅.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית: לפתוח את
  הדשבורד ולוודא ש"הכנסות החודש" תואם לרווח (לא להכנסה הגולמית מ-
  Sales.tsx); לפתוח פאנל הזמנה ולערוך את הסטטוס דרך ה-select החדש,
  כולל בחירת "אחר" והקלדת טקסט חופשי.

## הערה על git status

`summary2.md`/`summary3.md`/`summary4.md` עדיין מופיעים כ-`deleted`
ממשימות קודמות - לא נגעתי בהם, נשארים מחוץ לקומיט הזה.
