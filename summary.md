# סיכום: תיקון פורמט תאריך גורף - dd/mm/yyyy בכל מקום

## שיטת החיפוש

חיפוש גלובלי יסודי לפי שלושת הכיוונים שהתבקשו:
1. `grep` על `toLocaleDateString` בכל הפרויקט - אפס תוצאות מחוץ להערה
   ב-`formatDate.ts` עצמו (שמסבירה למה הוא *לא* בשימוש) - כל השימושים
   הקודמים כבר הוסרו בסבבים קודמים.
2. `grep` על `getDate()`/`getMonth()`/`getFullYear()` - כל התוצאות
   הן חישובי תאריך פנימיים (הפרשי שבוע/חודש, מפתחות קיבוץ "YYYY-MM"
   לסינון) או הלוגיקה הפנימית של `DateInput.tsx`/`formatDate.ts`
   עצמם - לא תצוגה למשתמשת. לא נמצא שם מקום שדורש תיקון.
3. `grep` על כל תבנית `.date}`/`createdAt}`/`dueDate}` בכל קובצי
   ה-TSX - זה מה שחשף את הבאגים בפועל (ראו למטה). `dueDate` מאומת
   כלא-מוצג בשום מקום בכלל (רק נשמר) - לא היה שם מה לתקן.

## 4 מקומות שנמצאו ותוקנו - כולם הוחלפו לקריאה ל-formatDateIL הקיימת

- **`OrderDetailsPanel.tsx` שורה 331** - **זה המקום שהוזכר** (תאריך
  תשלום בהיסטוריית התשלומים): `{p.date}` הציג את מחרוזת ה-ISO הגולמית
  ("YYYY-MM-DD") כמו שהיא נשמרת ב-Firestore, במקום `dd/mm/yyyy`.
- **`OrderDetailsPanel.tsx` שורה 220** - "תאריך יצירה" של ההזמנה עצמה
  (`order.createdAt`) - אותו באג בדיוק.
- **`ClientDrawer.tsx`** - שני מקומות: תאריך בכרטיס הזמנה בלשונית
  "הזמנות ועבודות" (`ord.createdAt`), ועמודת התאריך בטבלת "פירוט לפי
  הזמנה" שנוספה בלשונית "תשלומים וחובות" בסבב הקודם (גם היא נכתבה עם
  אותו באג - תוקן עכשיו).
- **`Expenses.tsx`** - עמודת התאריך בטבלת ההוצאות (`e.date`) - אותו
  באג בדיוק, בכל שורת הוצאה בטבלה.

בכל 4 המקומות נוסף `import { formatDateIL } from "../../utils/formatDate"`
(לא נוצרה שום פונקציה חדשה) והוחלף השימוש הגולמי בקריאה אליה. כל
השדות שתוקנו (`createdAt`, `payment.date`, `expense.date`) נשמרים
כמחרוזת "YYYY-MM-DD" נטו (בלי שעה) - נופלים בדיוק לענף הפרסור המקומי
הבטוח של `formatDateIL` (regex על "YYYY-MM-DD" → `new Date(y,m-1,d)`
מקומי), בלי סיכון הזחה של יום מ-UTC.

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - אין שגיאות/אזהרות חדשות בשלושת הקבצים שנערכו.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית שכל
  התאריכים המתוקנים מוצגים כעת כ-dd/mm/yyyy עקבי.

## הערה על git status

`summary2.md`/`summary3.md`/`summary4.md` עדיין מופיעים כ-`deleted`
מהמשימות הקודמות - לא נגעתי בהם, נשארים מחוץ לקומיט הזה.
