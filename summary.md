# סיכום: קבוצה ג׳ - פאות תצוגה: הצגת נמכרו + פילטר (Inventory.tsx)

## 6. הצגת פאות שנמכרו + שליטה על התצוגה

השאילתה ל-`orders` (ה-`onSnapshot`) כבר שולפת מראש את **כל** ההזמנות
של העסק (לא רק פאות תצוגה, ולא רק כאלה שלא נמכרו) - לא נדרש שינוי שם.
מה שהשתנה הוא רק הסינון **client-side** בלשונית "פאות תצוגה":

- `showroomOrders` (הקיים, לא נמכרו בלבד) נשאר ללא שינוי - עדיין משמש
  את מספר הכרטיסייה למעלה ("פאות תצוגה (X)").
- נוסף `allShowroomOrders` (כל `isShowroomStock===true`, כולל נמכרו).
- נוסף `filteredShowroomOrders`, נגזר מ-`allShowroomOrders` לפי
  `showroomViewFilter` (`'unsold' | 'sold' | 'all'`, ברירת מחדל
  `'unsold'` - זהה בדיוק להתנהגות הקודמת).

**פילטר חדש** בראש הלשונית: שלושה כפתורי toggle ("במלאי"/"נמכרו"/"הכל"),
עיצוב זהה ל-`.tab-switch`/`.tab-btn` הקיימים (רק בקנה מידה קטן יותר).

**עמודת סטטוס מלאי חדשה** בטבלה: תג "במלאי" (ירוק, מחלקות `status-badge
status-available` הקיימות) או "נמכרה - {שם לקוחה}" (כתום, `status-sold`
הקיימת) - נעשה שימוש חוזר במחלקות התג שכבר קיימות בקובץ, לא נוצרו
חדשות.

**התנהגות לחיצה על שורה - התפצלה לפי סטטוס המכירה:**
- **לא נמכרה:** כמו היום - פותחת `ShowroomStockDetailsPanel` (עם כפתור
  "מכירה" הקיים בפנים - עדיין פעיל רק על אלה, כי הפאנל הזה נפתח רק
  עבורן).
- **נמכרה:** פותחת את `OrderDetailsPanel` **הרגיל** (אותו רכיב בדיוק
  כמו בהזמנה רגילה ב-Sales.tsx/ClientDrawer.tsx) - כי לפאה שנמכרה יש
  `clientId`/תשלומים אמיתיים, וכפתור "מכירה" כבר לא רלוונטי לה
  (`ShowroomStockDetailsPanel` גם לא מציג שם לקוחה/תשלומים בכלל - לא
  מתאים לפאה שכבר נמכרה). מומש בפועל (לא דולג) - כולל חיבור מחדש של
  `onOpenAssignHair` לאותו `AssignHairModal` המשותף הקיים.

**קבצים:** `src/pages/Inventory/Inventory.tsx`,
`src/pages/Inventory/Inventory.css`

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - אין שום אזהרה/שגיאה ב-`Inventory.tsx` (גם לא
  מהתבנית הקיימת/מתועדת של `react-hooks/set-state-in-effect`).
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית: למכור פאת
  תצוגה קיימת, לעבור לפילטר "נמכרו"/"הכל" ולוודא שהיא מופיעה עם התג
  הנכון ופותחת את OrderDetailsPanel בלחיצה, ושבפילטר "במלאי" (ברירת
  המחדל) ההתנהגות זהה לגמרי למה שהיה לפני השינוי.

## הערה על git status

`summary2.md`/`summary3.md`/`summary4.md` עדיין מופיעים כ-`deleted`
ממשימות קודמות - לא נגעתי בהם, נשארים מחוץ לקומיט הזה.
