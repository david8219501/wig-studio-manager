# סיכום: תיקון וולידציית מלאי חסרה ב-NewOrderWizard.tsx (סעיף 2 ב-REVIEW.md)

## הבעיה שתוקנה

`handleAddUsedBulkItem` לא בדקה שהכמות המבוקשת לא עולה על המלאי הזמין,
וגם אפשרה להוסיף את אותו פריט מלאי כמה פעמים ברצף לאותה הזמנה ולצבור
כמות שחורגת מהמלאי בפועל. בנוסף, `handleFinish` "טישטש" בשקט חריגות
כאלה עם `Math.max(0, ...)` בזמן הורדת המלאי בפועל - כלומר גם אם משהו
כן חמק, ההזמנה עדיין נוצרה עם מלאי שגוי בלי שום התראה.

## מה נעשה

### 1. `handleAddUsedBulkItem` - וולידציה בזמן הבחירה

הועתק בדיוק הדפוס הקיים כבר ב-3 מקומות באתר (`gramsExceedsStock`
ב-`AssignHairModal.tsx`, `bulkQtyExceedsStock` ב-`OrderDetailsPanel.tsx`,
והבדיקה המקבילה ב-`QuickRetailSaleModal.tsx`) - לא הומצאה גישה חדשה.

מחשבים את הכמות הכוללת שכבר נבחרה לאותו פריט קודם באותה הזמנה
(`usedBulkItems` המצטבר) + הכמות החדשה המבוקשת, ומשווים מול
`item.quantity` הזמין במלאי. אם חורג - מוצגת הודעת שגיאה
("הכמות המבוקשת עולה על המלאי הזמין - נותרו רק X יח'.") דרך
`bulkItemQtyError` (state חדש, מוצג עם `<span className="field-error">`
הקיים כבר בטופס), והפריט לא נוסף. השגיאה מתאפסת בכל שינוי ב-select
או בשדה הכמות, ובהסרת פריט.

### 2. `handleFinish` - הגנה נוספת (defense in depth) לפני יצירת ההזמנה

נוסף צ'ק מקדים, **לפני** הקריאה ל-`createOrderWithProductionExpense`:
מקבצים את `usedBulkItems` לפי `itemId` (סכום כמויות, למקרה שאותו פריט
מופיע כמה פעמים במערך), ומשווים כל סכום מול `bulkItemsCatalog` (המלאי
כפי שנטען כשהאשף נפתח). אם כמות כלשהי חורגת - **ההזמנה לא נוצרת בכלל**:
מוצגת הודעת שגיאה ברורה ב-`saveError`, ו-`setSaving(false)` +
`return` לפני כל כתיבה ל-Firestore.

זו רשת ביטחון למקרה קצה בלבד (למשל מכירה מקבילה אחרת שהורידה את
המלאי בפועל בין פתיחת האשף ללחיצה על "סיום") - הוולידציה האמיתית
כבר קורית בסעיף 1 בזמן הבחירה.

הוסר לגמרי ה-`Math.max(0, ...)` שקבע כמות שיורית ל-0 בשקט בבלוק
ההורדה בפועל של המלאי (ה-`Promise.all` אחרי יצירת ההזמנה) - מכיוון
שהצ'ק המקדים כבר מבטיח שלא תיתכן חריגה בשלב הזה, אין יותר צורך
"לתפוס" מספרים שליליים בשקט.

### 3. תיקון לוואי: כפילות כתיבה כשאותו פריט נבחר כמה פעמים

תוך כדי התיקון התגלה באג נוסף, קשור ישירות לאותו קטע קוד: בלוק
הורדת המלאי בפועל רץ בעבר עם `usedBulkItems.map(...)` - כלומר אם
אותו פריט נבחר פעמיים באותה הזמנה (למשל 3 יח' ואז עוד 4, סה"כ 7 מתוך
10 במלאי - תקין לפי הוולידציה), נוצרו **שני** `updateDoc` מקבילים על
אותו מסמך, כל אחד מחשב `remaining` מאותו `catalogItem.quantity` המקורי
(10-3=7 ו-10-4=6) בלי להתחשב באחר - כתיבה אחרונה "מנצחת" ומשאירה מלאי
שגוי (6 או 7, לא 3), תלוי בסדר סיום ה-promises. תוקן על ידי שימוש
באותו מיפוי מקובץ לפי `itemId` (`usedQtyByItemId`) גם כאן - כתיבה
אחת ל-`updateDoc` לכל פריט ייחודי, עם הסכום הכולל.

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל (זרימת בחירת פריטי מלאי +
  יצירת הזמנה) - מומלץ לבדוק ידנית לפני סמיכה מלאה, כמו בכל שינוי
  קודם בסבב הזה.
