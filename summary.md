# משימת לילה מקיפה - סיכום מרוכז סופי

בוקר טוב! רצתי ברצף מלא על כל 22 הפריטים בלי לעצור לאישור, כמו שביקשת.
כל קבוצה נבנתה, נבדקה (`build`+`lint`) ונדחפה בקומיט נפרד - שום דבר לא
אבד גם אם משהו היה נכשל באמצע. הפרוט המלא של כל פריט (מה נמצא, מה
הוחלט, למה) נמצא בהמשך הקובץ הזה למטה; כאן רק הסיכום המרוכז שביקשת.

## מה בוצע בפועל (14 פריטים, קבוצות 1/3/5)

**קבוצה 1 - Sidebar+Login** (`e1e07bd`): אות פתיחה אמיתית בעיגול הסיידבר,
כפתור עין לצפייה בסיסמה, פלייסהולדר אימייל עברי, וולידציית 6 תווים
מינימום, ותיקון באג אמיתי שנמצא - **לא היה `onAuthStateChanged` בכלל**
באפליקציה, כך שכל רענון דף החזיר גם משתמשת מחוברת למסך ההתחברות.

**קבוצה 3 - מלאי** (`615cc0a`): מזהה hairItems קצר יותר (H-1004-a1b
במקום HAIR-1004-a1b2), ברירת מחדל 'available' בטבלת מלאי שיער, תיקון
גלישה אופקית משותף ל-3 מודלים (שורש הבעיה: `.form-field` בלי
`min-width:0`+`width:100%`), שינוי שם "קופסת שאריות"→"קופסת איחוד
שיער" בכל מקום שהמשתמשת רואה, עריכה/מחיקה מלאות ל-bulkItems.

**קבוצה 5 - כללי** (`219b7e5`): עיגול לשלם בכל כרטיסי הסיכום העליונים
ב-4 עמודים (Sales/Dashboard/Expenses/Reports) - נמצא לא-עקבי שחלקם
כבר היו מעוגלים וחלקם לא.

## מה כבר היה מוכן מקודם (7 פריטים - נבדק ואומת, לא נגעתי שוב)

פריטים 2 (Sidebar businessName), 7 (רווח נטו בדשבורד), 8 (עריכת סטטוס
ב-OrderDetailsPanel), 17-18 (בהירות AssignHairModal), 21 (כותרת
דינמית), 22 (favicon) - כולם מסבבי עבודה קודמים באותה שיחה, אומתו
מחדש בקוד (grep, לא זיכרון) ונמצאו תקינים.

## מה נבדק ולא היה צריך תיקון (פריט 1)

**פריט 14** (ערכי דוגמה שנמחקים ב-AddHairModal) - נבדק בקוד ונמצא
**שהבאג המתואר לא קיים בכלל**: כל השדות כבר מתחילים מ-`''` אמיתי
עם placeholder אמיתי, לא value/defaultValue שנעלמים בהקלדה.

## מה נבדק ולא יושם, עם הסבר מדויק (פריט 1)

**פריט 19** (pagination/`limit()` לטבלאות גדולות) - שינוי ארכיטקטוני
גדול מדי לביצוע ללא בדיקה: שלושת העמודים (Sales/Calendar/Expenses)
תלויים בטעינת האוסף **המלא** דרך `onSnapshot` חי כדי לחשב סיכומים/
רווחים/חובות על פני **כל** הנתונים, לא רק "עמוד" מוגבל - `limit()`
אמיתי היה שובר את החישובים האלה או דורש שאילתה כפולה (בלי לחסוך שום
קריאה בפועל). המלצה מפורטת (כולל חלופה בטוחה יותר - "טען עוד" client-
side) תועדה בקבוצה 5 למטה.

## החלטות שקיבלתי לבד (לפי ההנחיה לא לעצור לשאול)

- **פריט 6** (הבאג המדויק לא היה ברור מהתיאור) - זיהיתי את הבעיה
  הקרובה ביותר שנתמכת בקוד (אין `onAuthStateChanged`) ותיקנתי אותה.
- **פריט 9** (איזה קיצור/פורמט) - `H-{num}-{3 תווים}`, עם רגקס תואם-
  לאחור כדי לשמור על מספור רציף.
- **פריט 15** (איזה שם חלופי) - "קופסת איחוד שיער".
- **פריט 16** (האם לאפשר עריכת כמות דרך טופס העריכה) - לא, בכוונה -
  יש כבר נתיב ייעודי ("הוספת מלאי") שגם רושם הוצאה ומעדכן עלות ממוצעת.
- **פריט 20** (מה בדיוק "כרטיסי סיכום עליונים") - כרטיסי ה-KPI/סיכום
  בראש כל עמוד, לא טבלאות פירוט שורה-בשורה.

## הצעה פתוחה מסבב קודם שעדיין מחכה לתשובה (לא קשורה למשימה הזו)

בסבב הקודם (רווח נטו בדשבורד) המלצתי לשנות את כותרת הכרטיס "הכנסות
החודש" ל"רווח החודש" (כי המספר מתחתיו כבר רווח נטו, לא הכנסה) - עדיין
לא בוצע, ממתין לאישור שלך.

---

## שלב 0: בדיקת מצב לפני תיקון (מבוססת קוד, לא זיכרון)

| # | פריט | מצב לפני התיקון |
|---|------|-------------------|
| 1 | Sidebar אות אמיתית | ❌ קבוע "אס" |
| 2 | Sidebar businessName | ✅ כבר בוצע (סבב קודם) |
| 3 | Login כפתור עין | ❌ לא קיים |
| 4 | Login פלייסהולדר אימייל | ❌ "name@example.com" באנגלית |
| 5 | Login וולידציית 6 תווים | ❌ לא קיים |
| 6 | באג התחברות חוזרת | ❌ אין `onAuthStateChanged` בכלל - כל רענון דף מציג מסך login מחדש גם למי שכבר מחוברת (ראו ניתוח למטה) |
| 7 | Dashboard רווח נטו | ✅ כבר בוצע (סבב קודם) |
| 8 | OrderDetailsPanel סטטוס עריך | ✅ כבר בוצע (סבב קודם) |
| 9 | מזהה hairItems קצר | ❌ עדיין `HAIR-{מספר}-{4 תווים מה-uid}` (ארוך) |
| 10 | ברירת מחדל טבלת שיער = available | ❌ ברירת מחדל היא `'all'` |
| 11-13 | גלישה אופקית ב-3 מודלים | ❌ נמצא באג אמיתי - ראו ניתוח למטה |
| 14 | ערכי דוגמה שנמחקים ב-AddHairModal | ✅ **לא היה קיים בכלל** - נבדק בקוד, `value=""` אמיתי ו-placeholder אמיתי כבר מלכתחילה |
| 15 | שם חלופי ל"קופסת שאריות" | ❌ עדיין השם המקורי בכל המקומות |
| 16 | עריכה/מחיקה ל-bulkItems | ❌ לא קיים - רק "−"/"הוספת מלאי"/"מכירה" |
| 17-18 | AssignHairModal בהירות | ✅ כבר בוצע (2 סבבים קודמים) |
| 19 | Pagination לטבלאות גדולות | נבדק - ראו החלטה למטה (לא יושם, תועד) |
| 20 | הסרת אגורות מכרטיסי סיכום | ❌ נמצא לא-עקבי: חלק מהכרטיסים כבר מעוגלים (למשל totalProfit ב-Sales.tsx), אחרים לא |
| 21 | document.title דינמי | ✅ כבר בוצע (סבב קודם) |
| 22 | favicon מותאם | ✅ כבר בוצע (סבב קודם) |

**מסקנה:** 6 פריטים כבר היו מוכנים (2,7,8,17,18,21,22 - למעשה 7, ראו תיקון
מספור), פריט אחד (14) התברר כלא-רלוונטי (הבאג המתואר לא היה קיים), פריט
19 נבדק ותועדה החלטה בלי יישום. שאר ה-14 פריטים בוצעו בפועל בסבב הזה.

---

## קבוצה 1: Sidebar + Login (6 פריטים) - הושלמה

### 1. עיגול סגול עם אותיות אמיתיות
`App.tsx`: אותו `getDoc` שכבר טוען `businessName` (מ-`users/{uid}`)
מורחב לטעון גם `firstName`/`lastName`, מחשב `userInitials` (אות ראשונה
מכל שם, למשל "אסתי כהן" → "אכ"), מעביר כ-prop חדש ל-`<Sidebar>`.
`Sidebar.tsx`: `sidebar-avatar` מציג `{userInitials || "אס"}` (נפילה
לברירת המחדל הישנה אם עדיין לא נטען).

### 2. שם עסק אמיתי - כבר בוצע בסבב קודם, לא נגעתי שוב.

### 3. כפתור עין לצפייה בסיסמה
`Login.tsx`: שני שדות הסיסמה (סיסמה + אימות סיסמה בהרשמה) עטופים
ב-`.password-field-wrapper` עם כפתור `Eye`/`EyeOff` (lucide-react,
כבר ספריית האייקונים של האתר) שמחליף `type="password"`/`"text"`.
הכפתור ממוקם בצד שמאל של השדה (לא לפי RTL העמוד) - השדה עצמו
`dir="ltr"`, אז זה הקצה הטבעי של תוכן הסיסמה המוקלד.

### 4. פלייסהולדר עברי לאימייל
`"name@example.com"` → `"כתובת האימייל שלך"`.

### 5. וולידציית סיסמה מינימום 6 תווים
נבדק לפני `onRegister` (יחד עם בדיקת אי-התאמת סיסמאות הקיימת):
`if (isRegistering && password.length < 6) { setLocalError(...); return; }`
- הודעת שגיאה עקבית עם הניסוח הקיים כבר ב-`App.tsx` למקרה ש-Firebase
עצמו דוחה (`auth/weak-password`).

### 6. תיקון באג "התחברות חוזרת מזוהה כיצירת עסק חדש"

**ניתוח (היה צריך להחליט לבד - לא ברור מהתיאור המדויק):** בדקתי את כל
הקוד סביב `isRegistering`/`handleLogin`/`handleRegister` - זה state
מקומי טהור ב-`Login.tsx`, שמתחיל תמיד `false` (מסך התחברות) בכל טעינה
מחדש של הקומפוננטה. לא מצאתי שום מקום שמאלץ אותו ל-`true` אוטומטית -
כלומר "מוצג בטעות כיצירת עסק חדש" ממש כפי שתואר לא נמצא ישירות.

**אבל נמצאה בעיה אמיתית וקרובה מאוד:** ב-`App.tsx` **אין בכלל**
`onAuthStateChanged` - `isLoggedIn` הוא state מקומי בלבד. המשמעות:
Firebase שומר את הסשן בעצמו (persistence), אבל **כל רענון דף** (או
פתיחה מחדש של הדפדפן) מאפס את `isLoggedIn` ל-`false` ומראה שוב את
מסך ההתחברות/הרשמה - **גם למי שכבר מחוברת בפועל** ויש לה סשן תקף.
זו בדיוק "התחברות חוזרת עם חשבון קיים שלא מזוהה נכון".

**התיקון שבחרתי:** נוסף `onAuthStateChanged(auth, ...)` ב-`App.tsx`
שמזהה סשן קיים אוטומטית ומעביר ישר למערכת בלי להראות שוב את מסך
ההתחברות. כדי לא "לתפוס" ולקצר את הודעת ההצלחה + ההשהיה המכוונת
(700-900ms) שכבר קיימת אחרי login/register ידניים - נוסף `useRef`
(`manualAuthRef`) שמסומן `true` בתחילת `handleLogin`/`handleRegister`;
המאזין החדש מדלג אם הדגל כבר `true` (מטופל כבר ע"י הזרימה הידנית).

**קבצים:** `src/App.tsx`, `src/components/Sidebar/Sidebar.tsx`,
`src/pages/Login/Login.tsx`, `src/pages/Login/Login.css`

### בדיקות (קבוצה 1)
- `npm run build` עובר נקי.
- `npm run lint` - רק אזהרת `react-hooks/set-state-in-effect` הקיימת/
  מתועדת (מופע נוסף על אותו effect), ושתי `no-explicit-any` שכבר היו
  קיימות ב-`catch (error: any)` הישנים. אין אזהרות חדשות ב-`Login.tsx`/
  `Sidebar.tsx`.
- לא בוצעה בדיקה ויזואלית בדפדפן - מומלץ לבדוק: להתחבר, לרענן את הדף
  ולוודא שנשארת מחוברת (לא חוזרת למסך login); לבדוק את כפתור העין
  בשני מצבי הטופס.

---

---

## קבוצה 2: דשבורד + OrderDetailsPanel (2 פריטים) - כבר בוצעה בסבב קודם

פריטים 7-8 (רווח נטו בדשבורד, עריכת סטטוס ב-OrderDetailsPanel) נבדקו
ואומתו כבר קיימים ועובדים - אין קוד נוסף לשנות. לא נוצר קומיט נפרד
לקבוצה הזו (אין שינוי).

---

## קבוצה 3: מלאי (8 פריטים) - הושלמה

### 9. מזהה hairItems קצר יותר להדפסה
`Inventory.tsx`: הפורמט השתנה מ-`HAIR-{num}-{4 תווים מה-uid}` (14 תווים,
למשל `HAIR-1004-a1b2`) ל-`H-{num}-{3 תווים}` (10 תווים, למשל
`H-1004-a1b`) - קיצור משמעותי לצורך כתיבה/הדפסה על תגית פיזית. הרגקס
שמחשב את המספר הבא (`maxNum`) תואם גם את הפורמט הישן (`^H(?:AIR)?-`),
כדי שהמספור יישאר רציף אחרי המעבר בלי סקריפט מיגרציה - פריטים ישנים
שומרים על ה-id הארוך שלהם (הוא ה-Firestore document key בפועל, לא
נוגעים בו רטרואקטיבית).

**שיקול שהוזכר בקוד:** קיצור ה-suffix מ-4 ל-3 תווים מקטין את שטח מניעת
ההתנגשות הבין-עסקית (16³ = 4096 צירופים במקום 16⁴ = 65536) - סביר
לגמרי למספר העסקים הריאלי במערכת כזו, לא סיכון משמעותי.

### 10. ברירת מחדל בטבלת מלאי שיער - available
`statusFilter` השתנה מברירת מחדל `'all'` ל-`'available'` - הצפייה
השכיחה ביותר (מה זמין בפועל לשיוך), לא כל הקוקוים כולל שנוצלו/נמכרו.

### 11-13. תיקון גלישה אופקית ב-3 מודלים

**הסיבה שנמצאה (משותפת לשלושת המודלים - קליטת קוקו/הוספת מוצר/מכירה
מהירה, כולם חולקים `.form-field`/`.modal-form-grid` מ-`Inventory.css`):**
ל-`<input>`/`<select>` בתוך `.form-field` לא היה `width: 100%` מפורש,
ול-`.form-field` עצמו (grid/flex item) לא היה `min-width: 0` - ברירת
המחדל `min-width: auto` מסרבת להתכווץ מתחת לרוחב התוכן המהותי. ב-
`QuickRetailSaleModal.tsx` יש `<select>` עם אפשרויות `{שם לקוח} ({טלפון})`
שיכולות להיות ארוכות - זה בדיוק מה שדוחף את הכרטיס לגלוש אופקית.

**התיקון:** נוסף `width: 100%` + `box-sizing: border-box` ל-`.form-field
input/select/textarea`, ו-`min-width: 0` ל-`.form-field` עצמו. גם נוסף
`overflow-x: hidden` ל-`.modal-card` כרשת ביטחון נוספת.

### 14. ערכי דוגמה שנמחקים ב-AddHairModal
**נבדק ולא נמצא באג** - `emptyForm` כבר מתחיל בכל השדות כ-`''` אמיתי,
וכל ה-`placeholder`-ים הם באמת placeholder (לא `value`/`defaultValue`)
שנעלמים בהקלדה כמצופה. לא בוצע שינוי - אין מה לתקן.

### 15. שם חלופי מקצועי ל"קופסת שאריות"
הוחלט על **"קופסת איחוד שיער"** - נשמע מקצועי יותר מ"שאריות" (קונוטציה
של פסולת) בעודו מתאר במדויק את הפעולה (איחוד כמה שאריות קוקו קטנות
לפריט אחד). שונה בכל מקום שהמשתמשת רואה בפועל: כפתור "+ צור קופסת..."
ב-`Inventory.tsx`, כותרת/placeholder ב-`CreateRemnantBoxModal.tsx`,
כותרת/הודעת שגיאה/label/הודעת ריק ב-`MergeRemnantModal.tsx`, ותוויות
הפריט ב-`AssignHairModal.tsx` (גם ברשימת הבחירה וגם ב-`hairItemLabel`
הנשמר לשיוכים **חדשים** - שיוכים ישנים שכבר נשמרו ל-Firestore לפני
השינוי שומרים על הטקסט הישן שלהם, זו תמונת מצב לא רטרואקטיבית, כמו
`costAtTime` וכל שדה "snapshot" אחר בקוד). **לא שונו:** שמות/טיפוסים
פנימיים (`isRemnantBox`, `RemnantMergeLogEntry`) והערות קוד - לא
user-facing, שינוי שם שם הוא רי-פקטור לא-נחוץ שלא התבקש.

### 16. עריכה ומחיקה למלאי פשוט (bulkItems)
`AddBulkItemModal.tsx` קיבל prop אופציונלי `editingItem` - כשמוגדר,
טוען את הערכים הקיימים (מלבד כמות - ראו הסבר), כותרת/כפתור משתנים
ל"עריכת פריט מלאי"/"שמירת שינויים", ושומר עם `updateDoc` על אותו `id`
(לא `setDoc` חדש). **שדה הכמות מוסתר בעריכה בכוונה** - יש לו כבר נתיב
ייעודי ("+ הוספת מלאי") שגם מעדכן עלות ממוצעת משוקללת וגם רושם הוצאת
רכישה; לאפשר שינוי כמות "שקט" דרך טופס עריכה היה עוקף את שתי הפעולות
האלה ופוגע בשלמות הנתונים.

`Inventory.tsx`: נוספו כפתורי ✏️/🗑️ לכל שורה בטבלת מלאי פשוט. מחיקה
עוברת דרך `ConfirmDialog` (`variant="danger"`) לפני `deleteDoc` בפועל.

**קבצים:** `src/pages/Inventory/Inventory.tsx`,
`src/pages/Inventory/Inventory.css`,
`src/pages/Inventory/AddBulkItemModal.tsx`,
`src/pages/Inventory/CreateRemnantBoxModal.tsx`,
`src/pages/Inventory/MergeRemnantModal.tsx`,
`src/components/orders/AssignHairModal.tsx`

### בדיקות (קבוצה 3)
- `npm run build` עובר נקי.
- `npm run lint` - אין אזהרות/שגיאות חדשות ב-`Inventory.tsx` (0 שורות
  ב-lint output). האזהרה החדשה היחידה בקובץ אחר (`AddBulkItemModal.tsx`
  שורה 30, על ה-`useEffect` החדש שמאתחל את הטופס) היא אותה תבנית
  `react-hooks/set-state-in-effect` קיימת/מתועדת בעשרות מקומות אחרים -
  לא סוג אזהרה חדש.
- לא בוצעה בדיקה ויזואלית בדפדפן - מומלץ לבדוק ידנית: ליצור קוקו חדש
  ולוודא שה-id הקצר נוצר נכון; לפתוח כל אחד משלושת המודלים במסך צר
  ולוודא שאין יותר גלילה אופקית; לערוך ולמחוק פריט מלאי פשוט.

---

---

## קבוצה 4: בהירות UX (2 פריטים) - כבר בוצעה בסבבים קודמים

פריטים 17-18 (כותרות משנה ברורות + הבהרת כפתור "הוסף שיוך" ב-
AssignHairModal) נבדקו ואומתו כבר קיימים ועובדים (`assign-hair-section-
title` עם "שיוכים קיימים"/"הוספת שיוך חדש" קיים בקוד). אין קוד נוסף
לשנות, לא נוצר קומיט נפרד לקבוצה הזו.

---

## קבוצה 5: כללי (2 פריטים) - הושלמה

### 19. Pagination/limit() לטבלאות הגדולות - נבדק, **לא יושם**

נבדק בקוד: שלושתן (`Sales.tsx`/`Calendar.tsx`/`Expenses.tsx`) טוענות
את **כל האוסף** של העסק דרך `onSnapshot` (בלי `limit()`), מסוננות רק
לפי `businessId`. הסיבה שזה **לא** "שינוי סביר וממוקד" (ולכן לא בוצע,
לפי ההנחיה המפורשת לתעד המלצה בלבד במקרה כזה):

1. **כל שלושתן מחשבות סטטיסטיקות/סיכומים על פני המערך המלא** - לדוגמה
   `totalRevenue`/`totalPaid`/`openDebt`/`totalProfit` ב-Sales.tsx,
   גרפי חודשים, סינוני טווח תאריכים. `limit()` אמיתי ברמת Firestore
   היה מגביל גם את הנתונים ששימשו לחישובים האלה - הסיכומים היו נהיים
   שגויים (רק על "העמוד" הנוכחי, לא הכל) אלא אם שומרים שאילתה שנייה
   נפרדת בלי הגבלה רק לצורך הסיכומים - כלומר לא באמת חוסך שום דבר
   (עדיין קוראים הכל בשביל הסיכומים), רק מוסיף מורכבות.
2. **onSnapshot לא תומך pagination הדרגתי טבעי** - `limit()`+`startAfter()`
   לרשימה חיה שגדלה "עמוד אחרי עמוד" דורש מעקב אחרי כמה מאזינים
   נפרדים או מעבר מ-listener חי ל-`getDocs` חד-פעמי (איבוד עדכון
   בזמן אמת).
3. שינוי כזה נוגע ב-3 עמודים מרכזיים בו-זמנית, כולל שינוי ארכיטקטורת
   הטעינה שלהם - מסוכן מדי לבצע ללא בדיקה/אישור.

**המלצה לעתיד (לא יושמה):** אפשרות בטוחה וממוקדת יותר - "טען עוד"
**client-side בלבד** על המערך שכבר נטען (בלי לשנות את שאילתת ה-
Firestore בכלל) - מציגים רק 30-50 השורות הראשונות מהמערך המסונן/
הממוין הקיים, עם כפתור שמרחיב. זה משפר ביצועי רינדור/DOM לעסק עם
הרבה הזמנות, **אבל לא מקטין את מספר הקריאות בפועל מ-Firestore** (עדיין
נטען הכל כדי לחשב את הסיכומים) - זה ההבדל המהותי מ-`limit()` אמיתי
שהוזכר בבקשה. אם המטרה היא לחסוך בעלויות/קריאות Firestore בפועל (לא
רק ביצועי רינדור) - נדרש refactor נפרד שמפריד "שאילתת סיכומים" (בלי
הגבלה) מ"שאילתת תצוגה" (עם `limit`), ומומלץ לעשות את זה כמשימה ממוקדת
נפרדת עם בדיקה בזמן אמת, לא כחלק ממשימת לילה.

### 20. הסרת אגורות מכרטיסי סיכום עליונים בכל הדפים

**נמצא לא-עקבי לפני התיקון:** ב-`Sales.tsx`, `totalProfit` כבר היה
מעוגל (`{ maximumFractionDigits: 0 }`) אבל `totalRevenue`/`totalPaid`/
`openDebt` לא - כלומר חלק מהכרטיסים כבר "נכונים" וחלק לא, תלוי איזה
ערך צבר במקרה שברירי שקלים מחישובים קודמים (למשל עלות שיער יחסית
למשקל). ב-`Reports.tsx`, "ממוצע הזמנה" כבר עוגל (`Math.round`) אבל
"סה"כ הכנסות שנתי"/"רווח משוער" לא.

**התיקון:** נוסף `{ maximumFractionDigits: 0 }` (או `Math.round`,
תלוי בסגנון הקיים באותו קובץ) לכל שאר הכרטיסים העליונים: `Sales.tsx`
(totalRevenue/totalPaid/openDebt), `Dashboard.tsx` (thisMonthRevenue/
openDebt), `Expenses.tsx` (totalExpenses/inventoryExpenses/
operationalExpenses), `Reports.tsx` (revenueThisYear/profitThisYear).
**רק בתצוגה** - שום חישוב/ערך שמור ב-Firestore לא שונה, רק העיגול
ב-`toLocaleString`/מחרוזת התצוגה. טבלאות פירוט שורה-בשורה (למשל טבלת
ההזמנות עצמה) לא נגעו - רק כרטיסי הסיכום העליונים כפי שהתבקש.

**קבצים:** `src/pages/Sales/Sales.tsx`, `src/pages/Dashboard/Dashboard.tsx`,
`src/pages/Expenses/Expenses.tsx`, `src/pages/Reports/Reports.tsx`

### בדיקות (קבוצה 5)
- `npm run build` עובר נקי.
- `npm run lint` - אין שום אזהרה/שגיאה חדשה בארבעת הקבצים שנגעתי בהם
  (0 שורות ב-lint output לכולם).
- לא בוצעה בדיקה ויזואלית - מומלץ לוודא שכרטיסי הסיכום בכל 4 העמודים
  מציגים מספרים שלמים (בלי `.XX`).

---

*(הקבוצות הבאות יתועדו כאן בהמשך, בלי למחוק את מה שכבר תועד למעלה)*
