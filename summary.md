# סיכום: פתרון סופי - לוח שנה עצמאי + CustomSelect, ותיקון יישור השורות

## למה זה היה צריך תיקון נוסף

הפתרונות הקודמים (תיקון ה-CSS של ה-input הנסתר, הסרת backdrop-filter)
טיפלו בגורמים אמיתיים, אבל עדיין הסתמכו על רכיבי דפדפן/מערכת הפעלה
מובנים (`showPicker()`, `<select>`) - שבחלק מהסביבות (בעיקר לינוקס,
ותוך iframe כמו בסביבת הפיתוח הזו) מתעלמים ממיקום השדה בעמוד לגמרי
ונפתחים כ"חלון מערכת" נפרד. הפתרון היחיד שבאמת נותן שליטה מלאה: להפסיק
להסתמך על רכיבים מובנים, ולבנות רכיבים עצמאיים שהמיקום שלהם מחושב
ונשלט לגמרי בקוד שלנו.

## 1. `DateInput.tsx` - לוח שנה גרפי עצמאי (לא native)

הוסר לגמרי המנגנון של `showPicker()` על input מוסתר. במקומו: לחיצה על
אייקון 📅 פותחת פאנל לוח שנה משלנו - כותרת עם חצי ניווט קודם/הבא בין
חודשים (תואם לכיוון החצים הקיים כבר בניווט השבועי של היומן), שורת
אותיות ימי השבוע, ורשת ימי החודש; לחיצה על יום בוחרת אותו וסוגרת.

**מיקום נשלט לגמרי בקוד**: הפאנל מוצג דרך `createPortal` ישירות
ל-`document.body`, עם `position: fixed` שה-top/left שלו מחושבים
ב-JS מ-`getBoundingClientRect()` של השדה עצמו בכל פתיחה - לא מסתמך
על שום CSS positioning context של הורים בעמוד, ולכן חסין לגמרי לכל
`transform`/`filter`/`overflow` שעלול להיות על אב כלשהו (בדיוק סוג
הבעיה שגרמה לתקלה הקודמת). נסגר בלחיצה מחוץ לפאנל, ב-Escape, ובגלילה/
שינוי גודל חלון (כדי לא להישאר "תלוי" במקום הישן).

## 2. `CustomSelect.tsx` (חדש, `src/components/common/`) - תחליף ל-`<select>`

אותו עיקרון בדיוק: כפתור שמציג את הערך הנבחר, לחיצה פותחת רשימה
(דרך אותו מנגנון portal + `getBoundingClientRect()`). רכיב גנרי -
מקבל `options: {value, label}[]`, `value`, `onChange` - זמין לשימוש
בכל מקום אחר באתר שיתקל באותה בעיה עם `<select>` מובנה. הוחלף בו
שדה "מטרת הפגישה" ב-`Calendar.tsx`.

## 3. תוקנה בעיית היישור - האבחון היה נכון

התווית "סיבת ההגעה / מטרת הפגישה *" הייתה ארוכה מדי לרוחב העמודה -
נשברה לשתי שורות, מה שדחף את השדה מתחתיה למטה לעומת שדה התאריך
(תווית קצרה יותר, שורה אחת). התיקון:

- **קיצור הטקסט** ל-**"מטרת הפגישה \*"** בלבד (הוסר "סיבת ההגעה / ") -
  קצר מספיק שלא נשבר גם ברוחב הצר.
- **הגנה נוספת ל-`.form-group label`**: `min-height: 32px` +
  `display:flex; align-items:flex-end` - כך גם אם טקסט ארוך יתווסף
  שוב בעתיד באותו מקום, שני השדות באותה שורה תמיד יתחילו מאותו קו
  אופקי (התווית "יושבת" בתחתית השטח השמור לה, בין אם שורה אחת ובין
  אם שתיים).

## בדיקות

`npm run build` (tsc -b + vite build) עובר נקי. `npm run lint` - אזהרה
חדשה אחת נוספה (ב-`DateInput.tsx`, אותו דפוס `react-hooks/set-state-in-effect`
הקיים כבר בעשרות מקומות אחרים בקוד - לא סטייה חדשה).

**הסתייגות כנה, כמו בסיבוב הקודם**: לא הצלחתי לבדוק ויזואלית בדפדפן
אמיתי בסביבה הזו. הפעם זה פחות משנה - הפתרון לא מסתמך יותר על ניחוש
לגבי התנהגות דפדפן/מערכת הפעלה ספציפית, אלא על מיקום מחושב ישירות
מ-`getBoundingClientRect()` בקוד שלנו, שהיא שיטה עקבית ואמינה בכל
דפדפן/סביבה.
