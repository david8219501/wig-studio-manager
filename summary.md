# סיכום: עיצוב מחדש חלקי של דף דוחות (Reports.tsx) - 3 תובנות חדשות

## קבוצה 0: בדיקת תצוגה של התוכן הקיים ✅ הושלמה - נמצאו ותוקנו בעיות אמיתיות

בדקתי את כל 4 סוגי הבאג שהתבקש לבדוק, מול התוכן הקיים (כרטיסי KPI +
טבלת "דו"ח מכירות מפורט" החודשית + "יעילות תפעולית"):

1. **התנגשות `.mono`/table-layout (יישור שבור) - נמצאה, תוקנה.**
   בדיוק אותה משפחת באג שכבר תוקנה היום ב-`ClientDrawer.css`/
   `Sales.css`: `.mono` הגלובלי (`index.css`) קובע `display: inline-
   flex`, ו-3 מתוך 6 העמודות בטבלה החודשית (הכנסות/הוצאות/רווח)
   מקבלות `mono` ישירות על ה-`<td>` - זה דרס את ה-`table-cell` שלהן
   ושבר את היישור מול שלושת העמודות האחרות (חודש/מספר הזמנות/שירות
   מוביל). תוקן עם סלקטור ממוקד `.reports-table td.mono { display:
   table-cell; }` (בלי `!important`, אותה טכניקה שכבר הוכחה).
   בנוסף, ל-`.reports-table td` לא היה `text-align: right` מפורש
   (הסתמך על ברירת מחדל RTL משתמעת) - נוסף במפורש, כמו בכל טבלה
   אחרת שתוקנה השבוע.

2. **גלישה אופקית - נמצאה חשיפה, תוקנה מנע.** הטבלה החודשית לא הייתה
   עטופה ב-container עם `overflow-x: auto` (בשונה מ-`Sales.tsx`/
   `Expenses.tsx` שכבר משתמשים בדפוס הזה) - במסך צר, זה יכול היה
   לגרום לגלילה אופקית של כל העמוד. נוסף `.reports-table-wrapper` עם
   `overflow-x: auto`.

3. **טקסט לועזי בלי `dir="ltr"` - לא נמצא.** התוכן הקיים לא מציג שום
   טקסט לועזי (אימייל/טלפון/שם לועזי) - אין מה לתקן כאן.

4. **מספרים לא מעוגלים - נמצא, תוקן.** כרטיסי ה-KPI כבר היו מעוגלים
   (`Math.round`, מתיקון קודם באותו יום), אבל **עמודות הכסף בטבלה
   החודשית** (הכנסות/הוצאות/רווח לכל שורה) לא היו - `row.income.
   toLocaleString()` בלי `Math.round`. נוסף `Math.round(...)` לשלוש
   העמודות.

**הערה שלא תוקנה (מחוץ לתחום, לתשומת לב):** `.reports-stats-grid`
(4 עמודות קבועות) ו-`.reports-main-grid` (2fr/1fr) אין להן אף
`@media` breakpoint לקריסה במסך צר - לא אחת מ-4 קטגוריות הבאג
שהתבקשו במפורש (זו בעיית responsive layout, לא table-layout/mono/
dir/עיגול), אז לא תוקנה בסבב הזה - רק מצוינת כפער קיים.

**קבצים:** `src/pages/Reports/Reports.tsx`, `src/pages/Reports/Reports.css`

**בדיקות:** `npm run build` נקי. `npm run lint` - 24 בעיות, זהה
לבייסליין הקבוע לאורך כל העבודה על הפרויקט היום - `Reports.tsx`
נקי לחלוטין (0 שורות).

## קבוצה 1: רווחיות לפי סוג עבודה - סך כולל, לאורך זמן ✅ הושלמה

נוסף גרף עמודות מוערם (stacked `BarChart` מ-`recharts`, אותה ספרייה
שכבר בשימוש ב-`Dashboard.tsx`) המציג, לכל אחד מ-12 החודשים
האחרונים, את סך הרווח הכולל (לא ממוצע) שכל אחד מ-4 סוגי העבודה
הביא: פאה חדשה / תיקון-שירות / מוצר קמעונאי / פאת תצוגה.

**מקורות נתונים:**
- 4 המחרוזות המדויקות של `orderType` אומתו בפועל בקוד (לא ניחוש) -
  `ORDER_TYPE_LABELS` ב-`NewOrderWizard.tsx`, וקבועים ישירים ב-
  `RepairOrderForm.tsx`/`ShowroomStockFormModal.tsx`/
  `QuickRetailSaleModal.tsx`.
- הרווח מחושב עם `calculateOrderProfit` (`orderProfit.ts`) - רווח
  נטו אמיתי (הכנסה פחות עלות ייצור בפועל), לא `totalPrice` גולמי.
- הזמנות עם `status === "בוטלה"` מוחרגות (קבוע מקומי `CANCELLED_
  STATUS`, לא מיובא - עקבי עם `OrderDetailsPanel.tsx` שבו קבוע זהה
  מוגדר מקומית ולא מיוצא).
- נבחר חלון של 12 חודשים (לעומת 6 החודשים בטבלה הקיימת) כדי
  להבדיל את התובנה החדשה "לאורך זמן" מהטבלה הקיימת שכבר נותנת
  תמונת מצב חודשית קצרה.

**עיצוב:** נעשה שימוש חוזר במחלקת `.chart-wrapper` הגלובלית
(מוגדרת ב-`Dashboard.css`, אין צורך להגדיר מחדש כי כל ה-CSS
מתאגד לחבילה אחת) ובאותו דפוס JSX בדיוק (`ResponsiveContainer` +
`CartesianGrid` + `Tooltip` + `Legend`) כמו הגרף הקיים ב-Dashboard,
עם צבע קבוע לכל סוג עבודה לעקביות מחודש לחודש.

**קבצים:** `src/pages/Reports/Reports.tsx` (לא נדרש שינוי ב-CSS).

**בדיקות:** `npm run build` נקי (אחרי תיקון שגיאת `TS6133` על ייבוא
`formatDateIL` מיותר שנוסף מוקדם מדי לקראת קבוצה 3 - הוסר). `npm
run lint` - 24 בעיות, זהה לבייסליין, `Reports.tsx` נקי.

## קבוצה 2: מלאי "מת" - תקוע מעל 60 יום ✅ הושלמה

נוסף listener חדש (`onSnapshot`) על `hairItems` (מסונן לפי
`businessId`, בדיוק כמו שלושת ה-listeners הקיימים ב-Reports.tsx),
וכרטיס טבלה חדש שמציג פריטי שיער "תקועים": `status === 'available'`,
`currentWeight === initialWeight` (לא נוצלו בכלל), ו-`createdAt`
ישן מ-60 יום (`DEAD_STOCK_DAYS_THRESHOLD` - קבוע קשיח בקוד, לא
נשלף מ-`businessSettings`, כמבוקש במפורש).

**החלטה שהתקבלה תוך כדי (לא הוזכרה במפורש בבקשה):** קופסאות שאריות
(`isRemnantBox: true`) מוחרגות מהחישוב. הסיבה: "לא נוצל בכלל" לא
רלוונטי סמנטית לפריט שכבר נוצר ממיזוג שאריות קיימות, וה-`costPrice`
שלו לא משמעותי (`types/index.ts` מציין במפורש שהמחיר האמיתי של
קופסת שאריות הוא `remnantTotalValue/currentWeight`, לא `costPrice`).

**תצוגה:** לכל שורה - מזהה (`hairCode || id`, דפוס הנפילה-לאחור
הקיים באתר), גוון/אורך, עלות (מעוגלת, `.mono`), ימים במלאי. מוין
מהתקוע ביותר לפחות. סכום עלות כולל (`Math.round`, `.mono`) מוצג
מעל הטבלה. נעשה שימוש חוזר ב-`.reports-table`/`.reports-table-
wrapper` הקיימים (אותה הגנת `overflow-x`/תיקון `.mono` מקבוצה 0).

**קבצים:** `src/pages/Reports/Reports.tsx`, `src/pages/Reports/Reports.css`
(נוספה `.dead-stock-total` בלבד).

**בדיקות:** `npm run build` נקי. `npm run lint` - נתפסה שגיאת
`react-hooks/purity` אמיתית (`Date.now()` נקרא בתוך גוף ה-`useMemo`
- פונקציה לא טהורה) - תוקנה ע"י שימוש חוזר ב-`now` (אובייקט `Date`
שכבר קיים בתחילת אותו `useMemo`) במקום קריאה נוספת. אחרי התיקון:
24 בעיות, זהה לבייסליין, `Reports.tsx` נקי.
