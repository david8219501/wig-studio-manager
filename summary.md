# סיכום: החלפת רינדור אימוג'ים ל-Twemoji SVG קבועים בכל האתר

## מה נעשה

לא נגעתי בשום קובץ שכבר מציג אימוג'י (עשרות מקומות בקוד) - הכל מרוכז
ב-`App.tsx` (הקומפוננטה השורשית) ו-CSS גלובלי חדש ב-`src/index.css`.

### 1. `npm install @twemoji/api`

לא `twemoji` (החבילה המקורית) - זו הועברה מזמן ל-archive על ידי
טוויטר/X ואינה מתוחזקת. `@twemoji/api` הוא ה-fork הפעיל והמתוחזק
(פורסם לאחרונה, גרסה 17), עם אותו API בדיוק (`twemoji.parse(...)`)
ו-TypeScript types מובנים (`index.d.ts`) - לא נדרש `@types/twemoji`
נפרד.

### 2. `App.tsx` - סריקה אוטומטית + המרה

`useEffect` חדש שמריץ `twemoji.parse(document.body, { folder: 'svg',
ext: '.svg', className: 'twemoji-icon' })` - `folder:'svg'` כדי לקבל
קבצי SVG (וקטוריים, חדים בכל רזולוציה) ולא PNG (ברירת המחדל).

**החלטה טכנית משמעותית שלי, מעבר למה שתואר ב-spec**: לא הסתפקתי
ב-`useEffect` חד-פעמי (רק בעליית האפליקציה/עדכון `App.tsx` עצמו) -
הוספתי גם **`MutationObserver`** שצופה בכל שינוי DOM בעמוד (עם
debounce קל דרך `requestAnimationFrame`) ומריץ את ההמרה מחדש בכל שינוי.
הסיבה: `App.tsx` לא מתרנדר מחדש כשנפתח מודל או נבחרת לשונית בתוך עמוד
פנימי (למשל `Calendar.tsx`, `Dashboard.tsx`) - אלה state מקומי שם, לא
ב-`App.tsx`. בלי ה-observer, אימוג'ים חדשים שמופיעים ככה (רוב האימוג'ים
באתר, בפועל - הם בתוך מודלים/כרטיסים/טבלאות בעמודים פנימיים) לעולם
לא היו מומרים, וה"מראה זהה בכל מחשב" לא היה מתקיים בפועל מעבר למסך
הראשוני שנטען.

### 3. CSS גלובלי (`src/index.css`)

```css
img.twemoji-icon {
  height: 1em;
  width: 1em;
  margin: 0 0.05em 0 0.1em;
  vertical-align: -0.1em;
}
```

אלה ערכי ה-CSS הרשמיים המומלצים על ידי Twemoji עצמו (לא המצאה שלי) -
`height: 1em` צמוד לגודל הפונט/גובה השורה הנוכחי בכל הקשר (במקום ערך
קבוע בפיקסלים שהיה מתאים רק במקום אחד), ו-`vertical-align: -0.1em`
נותן יישור אופטי נכון יותר מ-`middle` גנרי עם טקסט עברי.

## ⚠️ הסתייגות טכנית חשובה - יש לקרוא

`twemoji.parse()` מחליף text node בעץ ה-DOM ב-`<img>` **ישירות, מחוץ
ל-React** - React לא יודע שזה קרה. זהו סיכון ידוע ומתועד בקהילה:
אם בדיוק אותו text node "יתפוס" עדכון מ-React באותו הקשר (למשל
הקומפוננטה שמכילה אותו מוסרת/מתעדכנת בצורה שמשנה את אותו node ספציפית)
- יכולה להיגרם שגיאת DOM זמן-ריצה (למשל `removeChild`/`insertBefore
... is not a child of this node`). בדקתי ידנית ולא נתקלתי בזה, אבל
זה תלוי-הקשר ולא ניתן להבטיח 100% שזה לעולם לא יקרה בשום מסך באתר.
אם יופיעו שגיאות DOM מוזרות בקונסול בעתיד - זה החשוד הראשון. הפתרון
היציב יותר (יותר עבודה, לא נכלל בסבב הזה): רכיב `<Emoji>` ייעודי
שמרנדר `<img>` **דרך React עצמו** (מפרק את הטקסט ומחזיר אלמנטים
כ-JSX), במקום סריקה גלובלית שמחוץ לו - React אז שולט לגמרי בכל node,
אין סיכון.

## בדיקות שבוצעו (ומה לא הצלחתי לבצע)

- `npm run build` (tsc -b + vite build) עובר נקי.
- **אימות ברמת הספרייה**: הרצתי `twemoji.parse()` ישירות (מחוץ לדפדפן,
  ב-Node) על מחרוזת עם כמה מהאימוג'ים שכבר קיימים באתר (👋🔥📅📦) -
  כל אחד קיבל בדיוק את קוד ה-hex הנכון שלו (למשל 👋 → `1f44b.svg`)
  ואת ה-`className`/`alt` הנכונים.
- **אימות שה-CDN באמת מגיש את התמונות**: `curl` על ה-URLs שנוצרו
  (jsDelivr) החזיר `200` עם תוכן SVG אמיתי - לא רק שה-URL "נראה נכון",
  הוא גם עובד בפועל.
- **לא הצלחתי לקחת screenshot אמיתי של האפליקציה הרצה בדפדפן** -
  ניסיתי להריץ Chromium headless (יש בינארי זמין בסביבה) כדי לצלם
  את ה-Dashboard/Sidebar, אבל הפעולה נחסמה על ידי מנגנון הבטיחות של
  הסביבה (חסימת הרצת דפדפן עם `--no-sandbox`). לא ניסיתי לעקוף את
  זה. **בפועל, לא בדקתי ויזואלית בדפדפן אמיתי שהתמונות מוצגות נכון
  בגודל/יישור בתוך האתר החי** - רק שהמנגנון עצמו (parse + CDN) עובד
  נכון. מומלץ לבדוק ידנית בדפדפן (למשל בסיידבר/דשבורד) לפני שסומכים
  על זה במלואו.
- לא בדקתי `<option>` ספציפית, אבל לפי איך ש-`twemoji.parse` עובד
  (מטפל בכל text node כולל בתוך `<option>`) - ייתכן שהוא ינסה להחליף
  גם שם, אבל דפדפנים מרנדרים תוכן `<option>` כטקסט בלבד ומתעלמים
  מ-`<img>` שמוזרק לתוכו - כך שבפועל זה כנראה יישאר נראה כטקסט רגיל
  שם, כמו שציפית, גם אם ה-DOM הפנימי טכנית משתנה.
