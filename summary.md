# סיכום: 2 תיקונים הפוכים לבקשות הקודמות

הבהרה חשובה: שני התיקונים האלה **מבטלים** שינויים מהסבב הקודם, לפי
הנחיה מפורשת שהם היו טעות/לא מה שהתבקש בפועל.

## 1. הסרת "קלאסי" מ-STRUCTURE_OPTIONS (לא הוספה)

בסבב הקודם נבדק (ונמצא תקין) ש-STRUCTURE_OPTIONS כולל "קלאסי" - אבל
זו הייתה הטעות: "קלאסי" שייך רק ל-FULLNESS_OPTIONS (מלאות:
דליל/קלאסי/מלא), לא למבנה. `src/utils/hairCost.ts`:
- `STRUCTURE_OPTIONS`: `["טופ", "סקין", "קלאסי", "סרט"]` →
  `["טופ", "סקין", "סרט"]`.
- `STRUCTURE_MOD` (טבלת המכפילים לחישוב עלות שיער לפי מבנה): הוסר
  `קלאסי: 0` משם גם כן. `טופ: -50`, `סקין: -40`, `סרט: 0` - שלושת
  הערכים הנשארים **לא השתנו**, נשארו בדיוק כמו שהיו.
- `FULLNESS_OPTIONS`/`FULLNESS_MOD` (ששם "קלאסי" כן שייך) - לא נגעתי
  בהם בכלל.

כל שלושת מקומות השימוש (`NewOrderWizard.tsx`, `Calculators.tsx`,
`ShowroomStockFormModal.tsx`) מייבאים ישירות מ-`STRUCTURE_OPTIONS`
המשותף, אז השינוי מתעדכן אוטומטית בכולם בלי לגעת בהם.

## 2. דאבל-קליק על כרטיסיית סוג הזמנה = בחירה + מעבר לשלב הבא

בוטל: ה-debounce guard (`lastTypeSelectRef`, ה-400ms, וההערות
שתיארו אותו כתיקון "באג לחיצה כפולה") שנוסף בסבב הקודם - זה לא היה
באג, זו הייתה בקשת פיצ'ר. הוסר לגמרי מ-`NewOrderWizard.tsx` (כולל
ה-`useRef` import שכבר לא נחוץ, ופונקציית העטיפה `handleTypeSelect`
שרק תיעדה את חותמת הזמן).

נוסף במקומו: `onDoubleClick` על שלוש כרטיסיות הבחירה בשלב 1 (פאה
חדשה/תיקון/פאת תצוגה) - קורא ל-`handleTypeSelectAndAdvance(type)`
חדשה, שקובעת את הסוג **וגם** קוראת מיד ל-`handleNext`. קליק בודד
(`onClick`) ממשיך להתנהג בדיוק כמו קודם - רק `setOrderType`, בלי
להתקדם.

**נקודה טכנית חשובה:** `handleNext` קיבל פרמטר אופציונלי חדש
`overrideOrderType` - כי `setOrderType` אסינכרוני (React state), אז
קריאה מיידית ל-`handleNext()` מיד אחרי `setOrderType(type)` הייתה
עדיין רואה את ה-`orderType` **הישן** מה-closure של הרינדור הנוכחי,
לא את הסוג שזה עתה נבחר בדאבל-קליק. `handleTypeSelectAndAdvance`
מעביר את הסוג במפורש (`handleNext(type)`), ו-`handleNext` משתמש
ב-`effectiveOrderType = overrideOrderType ?? orderType` בכל הבדיקות
הפנימיות שלו (יציאה ל"תיקון"/"פאת תצוגה" וכו') - כך שההתנהגות נכונה
גם בקריאה המיידית הזו וגם בקריאה הרגילה מכפתור "הבא" (`onClick={()
=> handleNext()}`, בלי פרמטר).

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - אין שגיאות/אזהרות חדשות ב-`NewOrderWizard.tsx`/
  `hairCost.ts`.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית שדאבל-
  קליק על כל אחת משלוש הכרטיסיות (כולל "תיקון"/"פאת תצוגה", שיוצאות
  מהאשף לגמרי) מתנהג נכון, ושקליק בודד עדיין רק בוחר בלי להתקדם.
