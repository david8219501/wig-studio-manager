# סיכום: תיקון "רווח החודש" + InfoTooltip גלובלי (2 חלקים)

## חלק א': תיקון נוסחת "רווח החודש" ב-Dashboard.tsx ✅ הושלמה

**ממצא לפני התיקון (שונה מהתיאור "כרגע" בבקשה):** הקוד **כבר** השתמש
ב-`calculateOrderProfit` (לא `totalPrice` גולמי), אבל **בלי שום
הפחתת הוצאות בכלל** (Dashboard.tsx לא טען `expenses` collection
בכלל) **ובלי החרגת הזמנות "בוטלה"**. כלומר "רווח החודש" כלל בטעות
גם הזמנות מבוטלות, ולא הפחית הוצאות תפעול בכלל - לא מה שהבקשה תיארה
כ"מצב קיים", אבל התוצאה הסופית שהתבקשה זהה בכל מקרה.

**התיקון:**
- נוסף מאזין Firestore חדש ל-`expenses` (לא היה קיים ב-Dashboard.tsx).
- `thisMonthRevenue`/`lastMonthRevenue` מחושבים כעת (`monthlyOperationalProfit`
  משותפת לשני החודשים):
  ```
  Σ calculateOrderProfit(order) [לחודש, status !== "בוטלה"]
  - Σ expenses.amount [לחודש, לא "מלאי וספקים"]
  ```
- **קטגוריית "מלאי וספקים" מזוהה נכון** (legacy `"inventory"` + החדש
  `"מלאי ושיער"`) - לא שוכפל inline; נוצרה פונקציה משותפת חדשה
  `isInventoryExpenseCategory` (`src/utils/businessSettings.ts`),
  ו-**גם `Expenses.tsx` עודכן להשתמש בה** במקום הבדיקה inline
  הקיימת (`inventoryExpenses` filter) - כדי שלא יהיו שני מקורות אמת
  לאותה בדיקה.

**קבצים:** `src/utils/businessSettings.ts` (פונקציה חדשה),
`src/pages/Dashboard/Dashboard.tsx` (`expenses` state+listener,
`CANCELLED_STATUS`, נוסחה מתוקנת), `src/pages/Expenses/Expenses.tsx`
(שימוש בפונקציה המשותפת במקום inline).

**בדיקות:** `npm run build` נקי. `npm run lint` - 24 בעיות, זהה
לבייסליין הקבוע.

## חלק ב': רכיב InfoTooltip גלובלי ✅ הושלמה

**רכיב חדש** `src/components/common/InfoTooltip.tsx` (+`.css`) -
אייקון ⓘ קטן, `props: { text: string }`. פותח על hover **וגם** על
לחיצה (toggle, לתמיכה במסכי מגע). בועית הטקסט נפתחת **למטה** (לא
מעלה) - החלטה מכוונת: אחד ממקומות השימוש הוא כותרת עמודה בטבלה עם
`overflow-x: auto` על ה-wrapper (Reports.tsx) - פתיחה מעלה הייתה
בסיכון להיחתך שם.

### 3 המקומות הוודאיים שהתבקשו
1. **Dashboard.tsx, "רווח החודש"** - הטקסט המבוקש בדיוק (מתאים
   למציאות אחרי תיקון חלק א').
2. **Reports.tsx, "רווח נקי" (עמודה בטבלה)** - **הטקסט שונה מהמוצע
   בבקשה במכוון**, ראו "אי-התאמה שתוקנה" למטה.
3. **Reports.tsx, "רווח משוער" (כרטיס עליון)** - נמצא (קיים בפועל),
   טקסט מקביל שנבדק מול הקוד בפועל.

### אי-התאמה שתוקנה (לא יושמה כמו שהתבקש, בכוונה)
הטקסט המוצע ל-"רווח נקי" ("רווח תפעולי להזמנה בלבד... לא מחסיר
הוצאות כלליות") **לא תואם את הקוד בפועל**: `row.profit` בטבלה הזו
מחושב כ-`income - exp` (הכנסה גולמית **פחות** כל ההוצאות של החודש,
כולל מלאי) - **לא** `calculateOrderProfit` לפי הזמנה. כתבתי טקסט
מדויק לפי מה שהעמודה **באמת** מחשבת, כדי שלא תהיה בועית הסבר
שמטעה (זה היה סותר את כל המטרה של הפיצ'ר). אותה נוסחה בדיוק חלה גם
על "רווח משוער" בכרטיס העליון - שתי התוויות מציגות בעצם את אותה
משפחת חישוב (הכנסה גולמית מינוס כל ההוצאות), **שונה** מ"רווח החודש"
בדשבורד (calculateOrderProfit מינוס הוצאות תפעול בלבד) - שני הטקסטים
מציינים את ההבדל הזה במפורש.

### 5 מקומות נוספים שנמצאו (עברתי על Dashboard/Sales/Reports)
4. **Sales.tsx, "יתרת חובות פתוחים"** - מחושב מ-`filteredOrders`
   (לפי הסינון הפעיל בטבלה) - **לא מוחרג הזמנות שבוטלו** כברירת
   מחדל.
5. **Sales.tsx, "רווח בפועל (משוער)"** - `calculateOrderProfit` על
   `filteredOrders` - גם כאן לא מוחרג בוטלה כברירת מחדל, ולא מחסיר
   הוצאות תפעול כלליות (שונה מ"רווח החודש" בדשבורד).
6. **Dashboard.tsx, "חובות פתוחים" (KPI)** - כמו #4, לא מוחרג בוטלה
   - **בניגוד** ל"חובות פתוחים לפי ותק" בדוחות (ראו #8), אז הסכומים
   בין הדפים עשויים להיות שונים בכוונה.
7. **Dashboard.tsx, "התפלגות סוגי עבודה"** - האחוזים לפי `totalPrice`
   (הכנסה גולמית), **לא** לפי רווח - התאמה מדויקת לדוגמה שנתת
   ("אחוז שירות מוביל... לפי הכנסה או רווח?").
8. **Reports.tsx, "חובות פתוחים לפי ותק"** - נוסף גם כאן (לא רק
   ב-#4/#6) כדי להבהיר את ה**הבדל**: כאן הזמנות "בוטלה" **מוחרגות**
   במפורש (בשונה מ-Sales/Dashboard) - בלי ההבהרה הזו, שלוש התוויות
   הדומות ("חובות פתוחים"/"יתרת חובות פתוחים"/"חובות פתוחים לפי
   ותק") עלולות להיראות כאותו מספר בדיוק כשהן לא.

**קבצים:** `src/components/common/InfoTooltip.tsx` (חדש),
`src/components/common/InfoTooltip.css` (חדש), `Dashboard.tsx`
(3 מקומות + import), `Reports.tsx` (3 מקומות + import, `tooltip?`
אופציונלי ב-`summaryStats`), `Sales.tsx` (2 מקומות + import).

**בדיקות:** `npm run build` נקי. `npm run lint` - 24 בעיות, זהה
לבייסליין הקבוע.
