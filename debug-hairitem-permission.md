תיקון: התנגשות ID בין עסקים שונים ב-hairItems.

הבעיה: ה-nextHairId (המזהה הידידותי הרציף כמו HAIR-1001) מחושב כנראה 
מתוך ספירה/סריקה של כל מסמכי hairItems הקיימים ב-collection, בלי סינון 
לפי businessId. עסק חדש, עם 0 קוקוים משלו, מקבל "מספר הבא" שכבר קיים 
כמסמך בפועל - השייך לעסק אחר (מנתוני בדיקה ישנים). setDoc על מזהה שכבר 
קיים הופך את הפעולה ל-"update" מבחינת Firestore (לא "create"), וחוקי 
העדכון בודקים את ה-businessId של המסמך הקיים (עסק אחר) מול uid העסק 
החדש - לא תואם, permission-denied.

התיקון: תבדוק את הלוגיקה שמחשבת את ה-nextHairId (סביר להניח ב-
Inventory.tsx, איפה שנקבע "ה-ID הבא"). היא חייבת לספור/לחפש רק בתוך 
הקוקוים ששייכים ל-businessId הנוכחי (מהמערך hairItems שכבר טעון ומסונן 
ב-state דרך onSnapshot הקיים), לא סריקה גלובלית של כל ה-collection.

תעדכן את החישוב כך שהוא בטוח נגזר רק מתוך הרשימה המסוננת-לפי-עסק שכבר 
קיימת ב-state, כך שלכל עסק יש רצף מספור עצמאי משלו, ולא יכול להתנגש עם 
עסק אחר לעולם (כי לכל עסק ה-Firestore Rules ממילא מבטיחים בידוד מלא - 
זה לא בעיה עסקית, רק בעיית ID גלובלי).

תריץ build, ותוודא בפועל (תבקש מהמשתמש לבדוק) שהוספת קוקו בעסק החדש 
עובדת אחרי התיקון. עדכן כשמסיים.

---

## התיקון שבוצע

בדקתי את `nextHairId` ב-`src/pages/Inventory/Inventory.tsx` (שורות 85-96
לפני התיקון) - החישוב **כבר** היה נגזר רק מתוך `hairItems` שב-state, וזה
כבר מסונן לפי `businessId` דרך ה-`query(... where('businessId', '==',
businessId))` ב-`onSnapshot` (שורה 52). כלומר לא הייתה כאן סריקה גלובלית
של כל ה-collection כמו שהנחת - המספור הסידורי (`maxNum`) כבר תמיד עצמאי
לעסק.

**אבל** האבחנה המרכזית שלך נכונה: `hairItems` הוא collection גלובלי אחד
המשותף לכל העסקים (לא subcollection נפרד לכל עסק), אז שני עסקים שונים
עדיין יכולים להגיע בדיוק ל**אותו מזהה מסמך** - למשל שני עסקים חדשים
שכל אחד מהם, בנפרד, מחשב "HAIR-1001" כ"מספר הבא" שלו. `setDoc` על מזהה
שכבר קיים כמסמך בפועל (של עסק אחר) הופך מבחינת Firestore ל"עדכון" ולא
"יצירה", וחוקי העדכון בודקים את ה-`businessId` הקיים מול ה-uid הנוכחי -
לא תואם, `permission-denied`. זה בדיוק תואם לתרחיש: עסק ראשון/ישן כבר
"תפס" את המספרים הנמוכים (1001, 1002...) מנתוני בדיקה ישנים, ועסק חדש
מתנגש איתם.

### מה שונה בקוד

ב-`nextHairId` (עדיין ב-`Inventory.tsx`):
- לחישוב `maxNum` - שינוי קטן ב-regex (`/^HAIR-(\d+)/` במקום
  `replace('HAIR-', '')`) כדי שימשיך לעבוד גם על מזהים עם הסיומת החדשה.
- נוספה **סיומת ייחודית לעסק** בסוף המזהה, שנגזרת מ-4 התווים האחרונים של
  `auth.currentUser?.uid`. כך המזהה הסופי הוא `HAIR-1001-jTg2` ולא סתם
  `HAIR-1001` - שני עסקים שונים כמעט אף פעם לא יגיעו לאותה סיומת+מספר
  יחד, כך שההתנגשות בפועל נעלמת, בלי לשנות את מבנה ה-collection הגלובלי
  ובלי לגעת בשום מקום אחר שמשתמש ב-`item.id` (עדיין אותו שדה, רק עם
  תוסף קצר בסוף).
- `AddHairModal` ו-`CreateRemnantBoxModal` שניהם מקבלים את אותו
  `nextHairId` כ-`nextId` prop וכותבים לאותו collection - התיקון חל על
  שניהם אוטומטית.

`npm run build` (tsc -b + vite build) עבר נקי.

### מה נשאר לאמת בפועל

תבדקי עם המשתמשת/העסק החדש: פתיחת "קליטת קוקו חדש" (או "קופסת שאריות
חדשה") ושמירה - אמורה לעבוד בלי `permission-denied`, וה"מזהה שיוקצה
אוטומטית" בטופס אמור להיראות כמו `HAIR-1001-xxxx` (עם סיומת של 4 תווים).