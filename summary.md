# סיכום: לשונית "תשלומים וחובות" בכרטיס הלקוחה - ניהול תשלומים אמיתי

## הבהרה חשובה - "הנתונים המדומים" כבר לא היו שם

לפני שינוי כלשהו, בדקתי: `client.price`/`client.paid` **לא קיימים
בכלל** בקוד הנוכחי - לא בטיפוס `Client` (`Clients.tsx`) ולא בשום
מקום אחר (`grep` גלובלי). כרטיסי הסיכום בלשונית "תשלומים וחובות"
כבר חישבו `totalPrice`/`totalPaid` **מ-`clientOrders` האמיתי**
(`clientOrders.reduce(...)`) לפני השינוי הזה. סביר שהממצא המקורי
ב-REVIEW.md כבר לא רלוונטי, או שתוקן בסבב קודם בלי שתועד כך במפורש.
עדכנתי רק את *התוויות* של הכרטיסים ("סה"כ עסקאות"/"שולם בפועל"/"יתרת
חוב" → "סה"כ חויב"/"סה"כ שולם"/"סה"כ חוב פתוח") כדי להתאים בדיוק
לניסוח שביקשת - הנתונים עצמם לא השתנו.

## פירוט לפי הזמנה - חדש

נוספה טבלה חדשה מתחת לכרטיסי הסיכום (`payments-orders-section`
ב-`ClientDrawer.css`) - שורה לכל הזמנה של הלקוחה (`clientOrders`,
כבר נטען בזמן אמת עם `where("clientId","==",client.id)`): תאריך,
סוג עבודה, מחיר כולל, שולם, חוב פתוח (מחושב `totalPrice - paidAmount`
לכל הזמנה בנפרד), סטטוס (עם `order-status-badge status-${status}` -
אותם צבעים בדיוק כמו ב-`OrderDetailsPanel.tsx`).

## לחיצה על הזמנה - OrderDetailsPanel הקיים, לא שכפול

`ClientDrawer.tsx` ייבא ישירות את `OrderDetailsPanel`/`AssignHairModal`
(אותם רכיבים בדיוק שכבר בשימוש ב-`Sales.tsx`) - שום ניהול תשלומים
לא נבנה מחדש. נוספו `selectedOrderId`/`assigningOrderId`, ונגזרים
`selectedOrder`/`assigningOrder` חי מ-`clientOrders` (לא state של
אובייקט שלם) - אותו דפוס בדיוק כמו `Sales.tsx`, כדי שהפאנל תמיד יראה
עדכון מיידי (למשל אחרי הוספת תשלום). לחיצה על שורה בטבלה החדשה
פותחת את הפאנל עם ניהול התשלומים המלא שכבר קיים שם.

## הזמנות ישנות בלי clientId

לא טופל בכוונה (כפי שהתבקש) - השאילתה הקיימת כבר מסננת לפי
`clientId === client.id`, אז הזמנות ישנות בלי `clientId` (רק
`clientName` טקסטואלי) פשוט לא יופיעו כאן, בלי מיגרציה.

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - אין שגיאות/אזהרות חדשות ב-`ClientDrawer.tsx`.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית שהטבלה
  מוצגת נכון, שלחיצה על שורה פותחת את הפאנל הנכון, ושהוספת תשלום
  משם משתקפת מיד גם בכרטיסי הסיכום שבראש הלשונית.

## הערה על git status

`summary2.md`/`summary3.md`/`summary4.md` (קבצי scratch ישנים מתחילת
הסשן) עדיין מופיעים כ-`deleted` ב-`git status` מהמשימה הקודמת -
לא נגעתי בהם, נשארים מחוץ לקומיט הזה כמו קודם.
