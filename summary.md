# סיכום: תיקון יישור בטבלת "פירוט לפי הזמנה" (ClientDrawer.tsx) - הסיבה האמיתית

## שלב 1: אבחון מדויק (לא ניחוש)

נספרו בפועל: `grep` על ה-JSX מצא **6 `<th>`** בשורת הכותרת מול **6
`<td>`** בכל שורת נתונים (`awk`/`grep` על בלוק ה-`clientOrders.map`) -
מספר תואם לחלוטין, אין חוסר התאמה מבנית. כלומר, בדיוק כפי שהמשתמשת
חזתה: זו בעיית CSS/רוחב עמודות, לא מבנה שגוי.

## שלב 2: הסיבה האמיתית שנמצאה

`src/index.css` (גלובלי לכל האתר) מגדיר:
```css
code, .mono {
  ...
  display: inline-flex;
  ...
}
```
**4 מתוך 6** ה-`<td>` בטבלה הזו (תאריך/מחיר כולל/שולם/חוב פתוח) מקבלים
`className="mono"` **ישירות על ה-`<td>` עצמו** (לא על span פנימי) - וזה
דורס את `display: table-cell` המובנה של התא, והופך אותו לתיבת
inline-flex שלא מתיישרת יותר כעמודת טבלה תקינה. שתי העמודות האחרות (סוג
עבודה/סטטוס) לא נושאות `mono` ונשארות `table-cell` רגיל - ומכאן בדיוק
תחושת "הנתונים לא מיושרים תחת הכותרות".

**זו לא תופעה חדשה בקוד הזה** - בדיוק אותה התנגשות כבר קיימת ב-Sales.tsx
(`<td className="cell-price mono ...">`), ושם היא כבר נפתרה מזמן ב-
`Sales.css` תחת ההערה "ULTIMATE TABLE LOCK FIX": `display: table-cell
!important` על `.sales-table th, .sales-table td`. הטבלה הזו
(`.payments-orders-table`) פשוט מעולם לא קיבלה את אותה תוספת.

(לשם השוואה: ל-`Clients.css`, שהמשתמשת ציינה כתבנית מוכחת, אין את הבעיה
הזו כלל כי היא בכלל לא משתמשת ב-`.mono` המשותף על ה-`<td>` שלה - יש לה
class ייעודי `.clients-table__phone` שקובע `font-family: var(--font-
mono)` ישירות, בלי לגעת ב-`display`.)

## התיקון

נוסף כלל ממוקד לטבלה הזו בלבד (לא נגיעה גלובלית ב-`.mono` שמשמש בעוד
עשרות מקומות באתר):
```css
.payments-orders-table td.mono {
  display: table-cell;
}
```
בלי `!important` - הסלקטור הזה (2 מחלקות + type) יותר ספציפי מ-`.mono`
הגלובלי (מחלקה בודדת), אז הוא מנצח בטבעיות מבלי לפרוץ קסקדה עם
`!important` (בשונה מהפתרון ב-Sales.css, שם היה צורך רחב יותר בכל מקרה
לחסימת flexbox גלובלי בכמה מקומות בבת אחת).

`table-layout`/`width`/`colgroup` ספציפיים - נבדק ולא נמצא כאלה כבר
מלכתחילה ב-`.payments-orders-table` - המבנה כבר תואם את הפילוסופיה
הפשוטה של `Clients.css` (`width: 100%`, `border-collapse: collapse`,
בלי קיבוע רוחב עמודות).

**קובץ:** `src/components/clients/ClientDrawer.css`

## בדיקות שבוצעו

- `npm run build` (tsc -b + vite build) עובר נקי.
- `npm run lint` - האזהרות שהופיעו ב-`ClientDrawer.tsx` הן על
  `useEffect`-ים קיימים מראש עם `react-hooks/set-state-in-effect`
  (לא נגעתי בקובץ ה-TSX כלל בתיקון הזה, רק ב-CSS) - לא קשור.
- לא בוצעה בדיקה ויזואלית בדפדפן בפועל - מומלץ לבדוק ידנית אחרי משיכת
  השינוי: לפתוח כרטיס לקוחה עם כמה הזמנות בלשונית "תשלומים וחובות",
  ולוודא שכל 6 העמודות (כולל תאריך/מחיר כולל/שולם/חוב) מיושרות תחת
  הכותרות הנכונות שלהן.

## הערה על git status

`summary2.md`/`summary3.md`/`summary4.md` עדיין מופיעים כ-`deleted`
ממשימות קודמות - לא נגעתי בהם, נשארים מחוץ לקומיט הזה.
