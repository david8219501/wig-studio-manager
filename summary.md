# סיכום: תיקון גלישת שדות בכרטיס "הוספת לקוחה חדשה" (שני הגרסאות)

## האבחון

בשתי הגרסאות (`AddClientModal.tsx`/`.css` העצמאי, וזו שבתוך `Calendar.tsx`
דרך `Calendar.css`) השורות עם שני שדות זה-לצד-זה (שם פרטי/שם משפחה,
טלפון/אימייל) משתמשות ב-flexbox/grid עם `flex: 1` / `1fr` - אבל
**לתאי flex/grid יש `min-width: auto` כברירת מחדל**, ששווה לרוחב
התוכן המינימלי (כולל ה-`<input>` שבפנים). זה מונע מהעמודות להתכווץ
מספיק כדי להיכנס לרוחב הכרטיס, וגורם לגלישה מחוץ לגבולות שלו - **גם
אם** ה-`<input>` עצמו מוגדר עם `width: 100%` ו-`box-sizing: border-box`
(שני אלה שולטים על גודל האלמנט *בתוך* התא, לא על גודל התא עצמו ביחס
לאבא ה-flex/grid).

## התיקון

- **`min-width: 0`** נוסף ל-`.form-group` (`Calendar.css`) ול-`.field`
  (`AddClientModal.css`) - זה מה שבאמת פותר את הגלישה, בכל רוחב כרטיס.
- **הורחב רוחב הכרטיס עצמו** בנוסף (לא רק כתיקון-מינימום): `.modal-content`
  ב-`Calendar.css` מ-`max-width: 500px` ל-`560px`; `.add-client-modal`
  ב-`AddClientModal.css` מ-`min(480px, ...)` ל-`min(560px, ...)`.
- **`box-sizing: border-box` אומת/הוסף במפורש** על `.modal-content`,
  על `.form-group input, .form-group select` (עם `width: 100%` נוסף
  שם גם כן), ועל `.add-client-modal`. (`.field__input` כבר היה עם זה
  קודם.) חשוב לציין: יש כבר `* { box-sizing: border-box; }` גלובלי
  ב-`src/index.css` שחל ממילא על הכל - ההוספה המפורשת היא רק להעלים
  ספק/לתעד את זה במקום, לא תיקון של פער אמיתי בגלובלי.

## בדיקות

`npm run build` (tsc -b + vite build) עובר נקי. לא נגעתי בשום קובץ
אחר מעבר לשני ה-CSS האלה - התיקון ממוקד לכרטיס "הוספת לקוחה חדשה"
בשתי הגרסאות שלו, כמו שהתבקש.
