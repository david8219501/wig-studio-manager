# סיכום: הסתרת/הצגת עמודת "רווח" בטבלת קטלוגי המחירים (Calculators.tsx) ✅ הושלמה

נוסף כפתור טוגל ("👁 הצג רווח" / "🙈 הסתר רווח") מעל רשימת הקטלוגים
השמורים בטאב "קטלוגי מחירים" - כשמוסתר, עמודת הרווח לא מוצגת בכלל
בטבלה (גם ה-`<th>` וגם כל ה-`<td>` נעלמים לגמרי, לא רק מטושטשים -
`showProfit && (...)` ולא `opacity`/`blur`).

**החלטת scope (state בודד לכל הטאב, כמו שהמשתמש הציע):** `showProfit`
הוא state בודד ב-`PriceCatalogsTab`, `false` כברירת מחדל ("מוסתר",
כדי שלא יחשף בטעות למי שמסתכל מעבר לכתף), וחל על **כל** הקטלוגים
בטאב יחד - לא כפתור נפרד לכל קטלוג. הסיבה: השימוש הטיפוסי הוא הצגת
המסך ללקוחה, ואין היגיון בלהראות רווח בקטלוג אחד ולהסתיר באחר בעת
ובעונה אחת כשהמסך מוצג לאותה לקוחה.

**החלטה נוספת (לא הוזכרה במפורש):** הטוגל **לא** חל על טבלת התצוגה
המקדימה במצב ידני בטופס יצירת קטלוג - זה כלי עבודה פנימי (העסק קובע
מחירים, לא מציג ללקוחה בשלב הזה), אז עמודת הרווח שם ממשיכה להיות
מוצגת תמיד כדי שהמשתמשת תדע כמה היא מרוויחה בזמן שהיא בוחרת מחיר
ידני. הטוגל חל רק על טבלאות הקטלוגים השמורים ברשימה שמתחת.

**קבצים:** `src/pages/Calculators/Calculators.tsx` (state `showProfit`,
כפתור טוגל, `<th>`/`<td>` מותנים), `src/pages/Calculators/Calculators.css`
(`.price-catalog-profit-toggle`).

**בדיקות:** `npm run build` נקי. `npm run lint` - 24 בעיות, זהה
לבייסליין הקבוע, `Calculators.tsx` נקי (מלבד 2 האזהרות הקיימות
מראש ב-`useMemo` של `PriceCalculator`/`RepairsCalculator`, לא נוגעות
לשינוי הזה). לא ניתן היה לבדוק ויזואלית בדפדפן בסביבה הזו (אין כלי
אוטומציית דפדפן זמין) - האימות מבוסס על build+lint נקיים וסקירת
קוד קפדנית, לא צילום מסך בפועל.
