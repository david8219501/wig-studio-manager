# סיכום: 3 תיקונים/תוספות לטאב "קטלוגי מחירים" (Calculators.tsx)

## 1. תיקון יישור בטבלת הקטלוג ✅ הושלמה

**חקירה קודם (כמבוקש, לא ניחוש):** השוואה מדויקת בין `.catalog-table`
(Calculators.css) לבין `.reports-table`/`.payments-orders-table`
(שכבר תוקנו היום ב-Reports.css/ClientDrawer.css) העלתה **בדיוק אותה
משפחת באג**: `.mono` הגלובלי (`index.css`) קובע `display: inline-
flex`, ותאי העלות/מחיר/רווח בטבלת הקטלוג מקבלים `mono` ישירות על
ה-`<td>` - זה דורס את ה-`table-cell` שלהם ושובר את היישור מול עמודת
האורך (שאין לה `mono`). `text-align: right` לא היה חלק מהבעיה בפועל
(מוגדר כבר על ה-`<table>` ועובר בתורשה ל-`th`/`td`).

**התיקון:** אותו פתרון מוכח בדיוק שכבר עבד היום, לא ניסוי חדש -
`.catalog-table td.mono { display: table-cell; }` (סלקטור ממוקד,
2 מחלקות+type, מנצח בלי `!important` - בדיוק כמו ב-`.reports-table
td.mono`).

**קבצים:** `src/pages/Calculators/Calculators.css`.

## 2. בחירת מצב תמחור: אוטומטי או ידני ✅ הושלמה

נוסף שדה `mode: "auto" | "manual"` לטופס יצירת קטלוג ולמסמך הקטלוג
עצמו ב-Firestore, עם מתג שני-כפתורים (לא dropdown) - נעשה שימוש חוזר
במחלקות הגלובליות `.tab-switch`/`.tab-btn` (כבר קיימות ב-`Inventory.css`,
בלי CSS חדש).

- **אוטומטי** (ברירת מחדל): `price = cost * (1 + profitMargin/100)`,
  ההתנהגות הקיימת ללא שינוי.
- **ידני**: בטופס היצירה מופיעה טבלת תצוגה מקדימה לכל 15 האורכים,
  עם שדה `price` הניתן לעריכה חופשית לכל שורה - ממולא כברירת מחדל
  עם ההצעה האוטומטית (לא 0). `cost` (עלות שיער גולמית) נשאר תמיד
  מחושב אוטומטית בשני המצבים - לא ניתן לעריכה ידנית, כמבוקש במפורש
  ("עובדה אובייקטיבית מהמחשבון, לא החלטת תמחור").

**כפתור "עדכן לפי הגדרות נוכחיות" (קיים):** עודכן להיות מודע-מצב
(`recomputeRows`) - `cost` מחושב מחדש תמיד בשני המצבים (עלות חומר
גלם יכולה להשתנות); `price` מחושב מחדש רק במצב אוטומטי, ובמצב ידני
נשאר כמו שהמשתמשת קבעה (מאותר לפי `length`, לא אינדקס, ליתר ביטחון).

**החלטה (לא הוזכרה במפורש):** שמירת עריכה (שם/מבנה/מלאות) של קטלוג
קיים משתמשת באותה `recomputeRows` מודעת-מצב - כי שינוי מבנה/מלאות
משפיע על `cost` הבסיסי בכל מקרה, ומצב ידני עדיין צריך להישאר עקבי
עם ה-`price` שהמשתמשת בחרה.

**תאימות לאחור:** קטלוגים שנוצרו לפני התיקון הזה (מהיום, ללא `mode`
וללא `rows[].price` - רק `suggestedPrice` הישן) ממופים בקריאה
כ"אוטומטי" עם `price` נופל בחזרה ל-`suggestedPrice`, כדי שלא יישברו
בתצוגה. זו לא הגנה ספקולטיבית - הפיצ'ר הזה כבר היה בפרודקשן מאותה
משימה קודמת היום, אז קטלוגים כאלה יכולים להיות קיימים בפועל.

**קבצים:** `src/pages/Calculators/Calculators.tsx`
(`CatalogRow`/`PriceCatalog`/`CatalogMode`, `computeAutoRows`,
`recomputeRows`, מתג המצב, טבלת תצוגה מקדימה למצב ידני),
`src/pages/Calculators/Calculators.css` (`.catalog-price-input`).

## 3. עמודת רווח לכל שורה ✅ הושלמה

נוספה עמודת "רווח" (`price - cost`) לכל שורת אורך, גם בטבלת הקטלוג
השמורה וגם בתצוגה המקדימה של מצב ידני בטופס היצירה - כדי שהמשתמשת
תמיד תראה כמה היא מרוויחה בפועל, גם כשקבעה מחיר ידני שסוטה מההצעה
האוטומטית. כותרת העמודה "מחיר מוצע" שונתה ל"מחיר" (נייטרלית למצב,
כי במצב ידני זה לא "הצעה" אלא הבחירה הסופית של המשתמשת). נוסף גם
תג מצב ("תמחור אוטומטי"/"תמחור ידני") לשורת המטא-דאטה של כל כרטיס
קטלוג, לצד המבנה/המלאות/תאריך היצירה.

**קבצים:** `src/pages/Calculators/Calculators.tsx` (עמודת `<td>`
רווח בשתי הטבלאות; `.text-success` הגלובלי הקיים מ-`Reports.css`
נעשה בו שימוש חוזר, בלי CSS חדש).

## בדיקות (לכל 3 השינויים)

`npm run build` נקי. `npm run lint`: תחילת ריצה עם `useEffect`
שמזין (seed) מחדש `manualPrices` בעת שינוי מבנה/מלאות גילתה שגיאת
`react-hooks/set-state-in-effect` אמיתית (`setState` סינכרוני בתוך
effect) - **תוקנה** ע"י הסרת ה-effect לגמרי: האיפוס עובר להיות
חלק מ-`onChange` של השדות עצמם (event handler, לא effect - התיקון
המומלץ בהודעת השגיאה של eslint), וברירת המחדל להצגה נופלת ל-
`row.price` (מהצעה האוטומטית) כשאין override שמור - בלי צורך למלא
מראש. אחרי התיקון: 24 בעיות, זהה לבייסליין הקבוע לאורך כל היום,
`Calculators.tsx` נקי (2 האזהרות הקיימות שם על `useMemo` של
`PriceCalculator`/`RepairsCalculator` קיימות מראש, לא נוגעות
לשינוי הזה).

**הערה:** לא ניתן היה לבדוק ויזואלית בדפדפן בסביבה הזו (אין כלי
אוטומציית דפדפן זמין) - האימות מבוסס על build+lint נקיים וסקירת
קוד קפדנית, לא צילום מסך בפועל.
