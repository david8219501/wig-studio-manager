# סיכום: 4 תיקונים לכרטיס "קביעת פגישה חדשה" ולכפתור ה-"+" ביומן

## 1. כפתור ה-"+" הועבר למקום הנכון

הוסר העיגול הזעיר מכותרת תא-היום. במקומו: כפתור ברור וקריא
**"+ הוסף פגישה"** (`.day-add-appointment-btn`, מלבני, מקווקו,
עם טקסט - לא איקון בלבד) בתחתית רשימת הפגישות בתוך הקובייה של כל יום
(`margin-top: auto` בתוך `.day-column-body` שהוא flex column - דוחף
את הכפתור לתחתית הקובייה תמיד, בין אם יש 0 פגישות ובין אם יש הרבה).
עדיין ממלא את התאריך של אותו יום אוטומטית (`handleOpenAddModalForDate`).

## 2. כפתור "+ הוסף פגישה" גם בתצוגה היומית

נוסף אותו כפתור, באותו עיצוב, בתחתית `.appointments-grid-daily`.
מכיוון שבתצוגה היומית מוצג רק יום אחד, התאריך שממולא הוא פשוט
`currentDateIso` (התאריך המוצג כרגע).

## 3. עקביות גובה: DateInput מול select

הבעיה: `<select>` ו-`<input>` עם אותו `padding` לא בהכרח מציגים אותו
גובה בפועל (הבדלי רינדור מובנים של הדפדפן בין השניים). התיקון: קבעתי
`height: 40px` מפורש (במקום להסתמך על `padding` בלבד) גם ל-
`.form-group input, .form-group select` ב-`Calendar.css` וגם ל-
`.date-input-text` ב-`DateInput.css` - אותו ערך בדיוק בשני המקומות,
כך שהשדה וה-select תמיד יהיו זהים בגובה, ללא תלות בהבדלי רינדור
מובנים.

## 4. תוקן: הבורר הגרפי וה-select נפתחו בראש הדף

נמצאו **שתי סיבות אמיתיות**, שתיהן תוקנו:

- **באג אמיתי ב-`DateInput`**: ה-input הנסתר ששולט על `showPicker()`
  היה עם `position: absolute; inset: 0; width: 1px; height: 1px;` -
  `inset: 0` (שמבקש למתוח לכל ה-wrapper) ו-`width/height: 1px` מפורשים
  **סותרים זה את זה**, ובחלק מהדפדפנים זה גורם לדפדפן לחשב עוגן שגוי
  לבורר התאריך. תוקן ל-`top:0; left:0; width:100%; height:100%` -
  תופס בדיוק את שטח ה-wrapper הגלוי, בלי סתירה.
- **`backdrop-filter: blur(4px)` על `.modal-backdrop`**: `filter`/
  `backdrop-filter` על אלמנט אב יוצר containing block חדש שידוע
  כגורם, בחלק מהדפדפנים (ובמיוחד בתוך iframe של תצוגה מקדימה, כמו
  בסביבת הפיתוח הזו), לתפריטי `<select>` להיפתח בעוגן שגוי - זה כנראה
  ההסבר לכך שגם ה-select "סיבת ההגעה" (שאין לו שום קוד מותאם משלי)
  הושפע. הוסר ה-`backdrop-filter`, והוגברה מעט האטימות של הכהיה
  (`rgba(0,0,0,0.4)` → `0.55`) כדי לפצות ויזואלית על אובדן טשטוש הרקע.

**הסתייגות כנה**: לא הצלחתי לבדוק ויזואלית בדפדפן אמיתי בסביבה הזו
(אין לי גישה לדפדפן חי) - שני התיקונים מבוססים על ניתוח קוד וזיהוי
באגים אמיתיים/גורמים ידועים, לא על צפייה ישירה בתוצאה. אם הבעיה
נמשכת אחרי זה, ייתכן שזו תופעה ספציפית לסביבת ה-iframe של Firebase
Studio שדורשת בדיקה נוספת.

## בדיקות

`npm run build` (tsc -b + vite build) עובר נקי.
